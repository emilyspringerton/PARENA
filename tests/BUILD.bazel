load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "test_lexer_parser",
    srcs = ["test_lexer_parser.c"],
    deps = [
        "//src:arena",
        "//src:ast",
        "//src:parser",
    ],
    # Matches gcc's own -Wall -Wextra -pedantic bar the Makefile/CI already
    # hold this codebase to (NORTHSTAR.md's own VS0 DoD names this exact
    # flag set for the C emitter's output; applying it to the compiler's
    # own source is the same discipline, not a new one).
    copts = [
        "-std=c99",
        "-Wall",
        "-Wextra",
        "-pedantic",
    ],
)

cc_test(
    name = "test_region",
    srcs = ["test_region.c"],
    deps = [
        "//src:arena",
        "//src:ast",
        "//src:parser",
        "//src:region",
    ],
    copts = [
        "-std=c99",
        "-Wall",
        "-Wextra",
        "-pedantic",
    ],
)

cc_test(
    name = "test_emit",
    srcs = ["test_emit.c"],
    deps = [
        "//src:arena",
        "//src:ast",
        "//src:emit",
        "//src:parser",
        "//src:region",
    ],
    copts = [
        "-std=c99",
        "-Wall",
        "-Wextra",
        "-pedantic",
    ],
)

# test_emit_ts -- real, v0 verification for the new TypeScript emitter (src/emit_ts.c), founder
# real-time 2026-08-30: "start working on the parena ts emitter using MISHRI as proving ground."
cc_test(
    name = "test_emit_ts",
    srcs = ["test_emit_ts.c"],
    deps = [
        "//src:arena",
        "//src:ast",
        "//src:emit_ts",
        "//src:parser",
        "//src:region",
    ],
    copts = [
        "-std=c99",
        "-Wall",
        "-Wextra",
        "-pedantic",
        "-Werror",
    ],
)
