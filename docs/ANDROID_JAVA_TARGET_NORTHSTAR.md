# NORTHSTAR — PARENA-compiled Java on a real Android app

**Moved (2026-09-03) to `github.com/emilyspringerton/SPIDERBEETLE` (`NORTHSTAR.md` there) — the
founder created a real upstream repo for this work, same real precedent `BURROW`'s own `DUNG.md`
set when `DUNG` got its own repo.** This file is kept as a pointer, not deleted, so existing
references from `STDLIB.md`/`EMILY/context/golden-docs-index.md` don't dead-end.

The real, current content — what's shipped (`BatteryUi.java`, compiled from this repo's own
`stdlib/android/battery_ui.prn`, verified with a real JDK), what's honestly not attempted (an
actual Android project — no Android SDK in this sandbox), and the real, concrete next steps — now
lives in `SPIDERBEETLE/NORTHSTAR.md`. `stdlib/android/battery_ui.prn` itself stays here in PARENA
(the reusable `.prn` source); the compiled `.java` output and any future real Android app code
live in `SPIDERBEETLE`.
