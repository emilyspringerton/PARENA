/* Generated by parena build -- VS0 domain 3, do not edit by hand. */
#include "parena_runtime.h"
#include <string.h>
#include <stdint.h>
#include <stdlib.h>
#include <math.h>

int commander_posture(int, int, int, int, int, int, int);

int commander_posture(int own_stocks __attribute__((unused)), int opp_stocks __attribute__((unused)), int own_damage_pct __attribute__((unused)), int opp_damage_pct __attribute__((unused)), int own_edge_dist __attribute__((unused)), int opp_edge_dist __attribute__((unused)), int edge_danger_threshold __attribute__((unused))) {
    if ((own_edge_dist < edge_danger_threshold)) {
    return 4;
    } else {
    if ((opp_edge_dist < edge_danger_threshold)) {
    return 3;
    } else {
    if ((own_stocks > opp_stocks)) {
    return 2;
    } else {
    if ((own_stocks < opp_stocks)) {
    return 1;
    } else {
    if ((own_damage_pct < opp_damage_pct)) {
    return 2;
    } else {
    if ((own_damage_pct > opp_damage_pct)) {
    return 1;
    } else {
    return 0;
    }
    }
    }
    }
    }
    }
}

