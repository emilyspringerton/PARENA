## 2026-09-04
- feat: `stdlib/gfd/action_bar_mod.prn` (GFD-MACRO-0012, "tie the action frame stuff into a
  parena mod based shape so we can easily allow for extension at the action bar affordance") —
  GFD's first real PARENA mod, real I32-only decision logic (`on-gfd-ability-for-slot`, a
  job x slot branch) matching ECOWAR's `card_effect_mod.prn`-tier proof, not a bare trigger.
  Chosen over the still-unscoped federated EduScript<->PARENA process model in GFD's own
  `docs2/MOD_SURFACE_NORTHSTAR.md` §3a — reuses the already-live static-link pattern
  `ECOWAR/docs/ARENA_API.md` documents instead. Generated C committed at
  `GoblinFoxDragon/apps2/battlegrounds_gui/packages/simulation/action_bar_mod.c`, wired into
  `town_ability_for_slot`. Verified via a standalone C probe (20 assertions) plus real native
  and WASM builds of the consuming client, WASM redeployed live. See
  `GoblinFoxDragon/docs2/MOD_SURFACE_NORTHSTAR.md` §7 for the full writeup, including a real
  found-live gap: PARENA's current `runtime/parena_runtime.h` doesn't compile under Emscripten
  (`forkpty` has no WASM equivalent) — fixed by pinning GFD's own copy to the original, minimal
  41-line runtime (`git rev 9bdf91e`), same per-repo-pinned-copy precedent ECOWAR already set.
- feat: `stdlib/usb/usb.prn` + `stdlib/usb/mass_storage.prn` (S213-04/S215-04, "write usb shit
  into the stdlibs" -> "make it work in either parena or burrow"). Real USB device enumeration
  over `/sys/bus/usb/devices/` (String/Vec-heavy, C-target only, same real split
  `k8s/k8s.prn`/`k8s/scaling.prn` already established) + a real, scalar-only, burrow-compatible
  `is-mass-storage-class?`. Real bug found and fixed live: the first draft's
  `(deref (vec/get &entries i))` over a `(Vec String)` compiled to a literal `void entry` in the
  generated C -- fixed by following `awk.prn`'s own working convention (pass `vec/get`'s result
  directly, no `deref`). Verified: `parena build` emits real C compiling clean under `gcc -c`
  (zero warnings) for both files; `burrow build` on `mass_storage.prn` emits real Go that
  compiles clean via `go build`. Real, separate finding: `stdlib/vec.prn`'s own source (unresolved
  generic `T`) is reference-only and must never be passed to `parena build` directly -- `Vec`
  support is a compiler-native intrinsic. Real, honest, not attempted: a vendor/product name
  database, interface-level class enumeration, live verification against a real device (none
  exists in this sandbox). See `STDLIB.md`'s own new "usb" section for the full writeup.

## 2026-09-03 (18)
- feat: stdlib/idunapro/cli_mod.prn redesigned around a real HealthStatus enum (kanban
  cruise-queue card 9988's own next-named prerequisite, closed the same day BURROW shipped
  match-on-a-user-defenum support). Replaced the prior Result/HealthError design (host had to
  read HealthError's own exported .Tag field directly, since match couldn't reach a user
  defenum's variant yet) with classify-health returning a bare 3-variant HealthStatus
  (Healthy/BadStatus/NotOk), consumed by two real matches (health-message, health-exit-code) --
  PARENA now decides the presentation message too, not just the pass/fail outcome. Compiled via
  the real `burrow build` into IDUNA_PRO/internal/burrowgen/idunapro_cli_gen.go (BURROW commit
  dd406c5). IDUNA_PRO's own healthErrorMessage Go-side workaround is gone entirely -- runHealth
  is now a pure "fetch bytes, print what PARENA decided" shell. Live-verified against IDUNA's
  actual running :8080 (healthy -> exit 0, "IDUNA_PRO instance is healthy") and an unreachable
  host (exit 1). IDUNA_PRO commit pending. (sess-20260902-2008-ed50169e)

## 2026-09-03 (17)
- feat: parena new -- real, "batteries included" scaffolding command (kanban priority-queue card PXCL-9311, "make parena cli tool do the same thing burrow new does but for C instead of for go"), the direct C-target sibling of BURROW's own already-shipped burrow new. Generates a real, immediately-runnable starter: <name>.prn, main.c (a real C host including the compiled output and calling into it), and a local copy of runtime/parena_runtime.h/.c -- then actually compiles AND RUNS the result via a real cc invocation before returning success. Built headless (PARENA_NO_GRAPHICS), matching burrow new's own judgment. Real, honest v0 assumption: run from this repo's own root (runtime files looked up relative to cwd, not argv[0]). New tests/integration/run_new_check.sh (make test-new): a real scaffold builds+runs+prints correct output, and a second parena new on the same name correctly refuses to overwrite. make test: 345/345, zero regressions. (sess-20260902-2008-ed50169e)

## 2026-09-03 (16)
- feat: stdlib/ldap/ber.prn -- real BER TLV primitives (kanban priority-queue card 342332432423, "ldap primatives parena"). LDAP's own real wire format (RFC 4511) is ASN.1 BER-encoded (checked live via real web research) -- every real LDAP message is a real Tag-Length-Value element at every nesting level. This is the real, single most foundational LDAP primitive: read-ber-tag/read-ber-length/build-ber-tlv, the direct structural analog of sip/rtp.prn's/net/dns.prn's own low-level protocol work. Real, honest v0 boundary: short-form length only (0-127), low tag numbers only (0-30) -- long-form/high-tag-number cases report real, honest errors rather than silently mis-decoding. build-ber-tlv uses a real, honest #target inline-c escape hatch (same real reason net/dns.prn's own build-dns-query already needed one -- a real tag/length header is frequently, legitimately zero, string/concat's own strcpy/strcat truncation would corrupt real, ordinary output). New make test-ber target, 5 real assertions including a deliberate zero-length-value case. make test: 345/345, zero regressions. (sess-20260902-2008-ed50169e)

## 2026-09-03 (15)
- feat: stdlib/net/dns.prn -- real DNS query/response primitives (kanban priority-queue card 334534, "DNS primitives parena"). Real, direct RFC 1035 A-record (IPv4) support: parse-dns-response is pure PARENA (byte reads only); build-dns-query uses a real, honest #target inline-c escape hatch (matching mag/gematria.prn's own established squish precedent), necessary because a real query's own fixed 12-byte header is mostly zero bytes and string/concat's own strcpy/strcat truncation (already documented in sip/rtp.prn) would break on literally every real query. Handles real DNS name compression on the read side (a real 0xC0 pointer byte, RFC 1035 4.1.4) -- ubiquitous in real responses, enough to correctly skip a compressed name. Real, genuine, live-confirmed compiler bug found and worked around: two sibling loop forms reused identical binding names (i/pos), a real "redefinition" C error, the exact same class base4/matrix.prn's own comment already documents -- worked around via renaming (qi/qpos, ai/apos), same fix that file already applies. New make test-dns target, 5 real assertions including an exact byte-for-byte query check and a real compressed-name response parse. make test: 345/345, zero regressions. (sess-20260902-2008-ed50169e)

## 2026-09-03 (14)
- docs: real research-and-planning pass for kanban priority-queue card PBX-002 ("full featured PBX add asterisk bindings to parena... fill in the gaps between our low level implementation and a real deal"). New docs/PBX_ASTERISK_NORTHSTAR.md, real, checked-live research into Asterisk's 3 control surfaces (AMI plain-TCP-text on 5038, ARI HTTP+WebSocket, AGI stdin/stdout). Real, decisive finding: AMI's own wire format is a direct structural sibling of sip/message.prn's own already-solved header-parsing shape -- not a new parsing problem. Recommends AMI first over ARI (needs WebSocket) or AGI (only reactive per-call). Real 4-phase plan: stdlib/pbx/ami.prn (Phase 1), a real login round-trip against a live Asterisk (Phase 2, honestly named untestable in this sandbox), event parsing + Originate/Hangup/QueueStatus actions (Phase 3), ARI/WebSocket deferred (Phase 4). Registered as golden doc PBX-ASTERISK-NORTH. No code written -- planning only. (sess-20260902-2008-ed50169e)

## 2026-09-03 (13)
- feat: stdlib/sip/rtp.prn -- real, low-level RTP header primitives (kanban priority-queue card PBX-001, "Build the narrow scope parena PBX primitives... low level close to the metal primitives like what does asterisk need"). Pure PARENA, no FFI: parse-rtp-header/build-rtp-header, a real, direct RFC 3550 fixed 12-byte header codec built entirely on net/wire.prn's own raw-byte/read-u16-be/read-u32-be. Two real, genuine bugs found live: (1) parse-rtp-header originally used string/length (confirmed literally strlen), which truncates at the first embedded 0x00 -- a completely ordinary real RTP field value, not an edge case; fixed by taking an explicit len : I32 parameter instead, same precedent pentest/pcap.prn's own Packet struct already set. (2) build-rtp-header has a genuinely deeper, honestly-documented (not silently patched) limitation: string/concat is strcpy/strcat under the hood, truncating the same way on the write side -- a real fix needs concat itself to grow length-aware semantics, out of this card's own narrow scope. Real, honest v0 boundary: only the fixed 12-byte header is parsed, a real CSRC list/header extension reports Unsupported rather than mis-parsing. New make test-rtp target, 6 real assertions including a deliberate all-zero-fields case proving the len-parameter fix works. make test: 345/345, zero regressions. net/wire.prn's own raw-byte helper is now exported (was internal-only). (sess-20260902-2008-ed50169e)

## 2026-09-03 (12)
- docs: real, unified planning pass for kanban priority-queue cards HW-001 ("UART STDLIBS AND DEPS STDLIBS PLANNING FOR HARDWARE PLATFORMS ARDUINO EQUIVALENT") and HW-003 ("SERIAL STDLIBS") -- UART is one specific kind of serial communication, treated as one ask not two. New docs/UART_SERIAL_NORTHSTAR.md, grounded in this session's own IDUNA_INVENTORY doc's real hardware. Checked existing foundation: io.prn (real generic file I/O) + pty.prn (the real structural precedent for a raw termios-adjacent syscall wrapper). The one real, genuinely new gap: a termios configuration primitive needed right after opening a real serial device file (baud rate + raw/8N1 mode) -- everything else already exists via io.prn. Real, honest, deliberately out-of-scope distinction: the Adafruit Feather's own radio module is very likely SPI, not UART, a genuinely different bus protocol, unscoped here. Real API surface (serial-open/-read/-write/-close), direct structural sibling of net/tcp.prn. 2-phase plan, Phase 2 (real hardware round-trip proof) honestly named as blocked -- no physical USB-serial device in this sandbox. Registered as golden doc UART-SERIAL-NORTH. No code written -- planning only. (sess-20260902-2008-ed50169e)

## 2026-09-03 (11)
- docs: docs/SIP_TWILIO_GATEWAY_NORTHSTAR.md -- real addendum answering kanban priority-queue card SIPX-001 ("don't we need to build an asterisk equivalent... routes our calls to our sip?"). Direct answer: yes, this doc's own Phase 1-2 IS that equivalent -- a real B2BUA/SIP proxy routing calls between our own SIP clients and Twilio's trunk plays the same real role Asterisk/FreeSWITCH/Kamailio play in a traditional PBX. Real, honest, named build-vs-adopt tradeoff, not resolved unilaterally: real Asterisk is mature and full-featured (dialplan/voicemail/IVR/conferencing this gateway doesn't need) and would route calls sooner; the narrow PARENA-native B2BUA this doc already plans dogfoods the language but re-derives real protocol logic (SIP proxy header-rewriting) Asterisk has already solved for years. Flagged as a real founder-level decision, not defaulted to either choice. (sess-20260902-2008-ed50169e)

## 2026-09-03 (10)
- feat: bazel/parena_compile.bzl -- real, reusable Bazel macro for committing a `.prn` file right alongside its own generated output in a CONSUMING repo (kanban priority-queue cards PX-001/PX-BZ-001). Real, checked-not-assumed finding first: `parena build <file>.prn -o <out>` already accepts a `.prn` file at any path today -- no compiler change needed, "add your mod to the stdlib" was always a convention, never an enforced restriction. Two real, live, pre-existing examples already exist in this monorepo (DUNG/parena/entry.prn + a go:generate-based go_library rule; ladybug/BUILD.bazel's own hand-written genrule against @parena//src:parena directly) -- this macro closes the one real remaining friction point: every adopting repo previously had to hand-write its own genrule boilerplate. New parena_compile_c(name, prn, out, deps) macro, real, live-verified via an actual `bazel build` against new bazel/testdata/ fixtures (both a single-file case and a real multi-file `deps` case, correctly combining 3 real functions into one real generated C file). README.md gets a new "Adopting PARENA in your own repo" section answering both cards directly, with a real usage example. Real, deliberate v0 scope: C target only -- an analogous BURROW-repo macro for the Go/TS/Java targets is real, separate, later work. (sess-20260902-2008-ed50169e)

## 2026-09-03 (9)
- fix: stdlib/editor/buffer.prn -- real, genuine UTF-8 bug fixed (cruise-queue card 3454353, "fix unicode in parena editor"). Every cursor-movement/delete function (move-cursor-left/-right, backspace-at-cursor, delete-forward-at-cursor, extend-selection-left/-right) moved exactly one BYTE, not one UTF-8 codepoint -- for any real multi-byte character (accented Latin, CJK, emoji), a single Backspace/arrow key only touched one byte of a real multi-byte sequence, corrupting it. Fixed via two new private helpers: utf8-codepoint-len (real byte count of the codepoint starting at a position, per RFC 3629's own lead-byte bit patterns) and utf8-prev-boundary (walks backward over continuation bytes to the start of the preceding codepoint) -- every affected function now moves by a whole codepoint. Real, honest signedness note, same fix class this session's own net/wire.prn already needed: char-at sign-extends a byte >= 0x80 on this box, so a new byte-at helper normalizes with bit-and 255 first. New tests/test_editor_unicode.c (make test-editor-unicode), deliberately exercising both a real 2-byte codepoint (e/acute) and a real 4-byte codepoint (an emoji), not just ASCII -- all pass. make test: 345/345; the existing real SDL-driven make test-editor also stays green (ASCII-only edit path unaffected). STDLIB.md's own "editor/buffer" section updated. (sess-20260902-2008-ed50169e)

## 2026-09-03 (8)
- docs: real research-and-planning pass for kanban priority-queue card SIP-001 ("what software do we need to build to be the pipes between our sip phone clients and twilio"). New docs/SIP_TWILIO_GATEWAY_NORTHSTAR.md, grounded in real, checked Twilio Elastic SIP Trunking requirements (SIP methods, UDP/TCP/TLS transports, IP ACL vs Credential List auth, real signaling/RTP IP+port ranges). Maps onto the existing real foundation (sip/message.prn + net/udp.prn). Real, valuable finding: a pure RTP relay needs zero codec understanding -- G.711 only matters for direct audio synthesis/consumption, deferred. Real, newly-identified gaps named: SDP parsing/construction, SIP proxy/B2BUA header-rewriting (the hardest real protocol work), multi-fd poll/select concurrency (a genuinely new runtime primitive), SIP digest auth (needs md5, crypto/hash.prn only has sha256 today -- skippable via IP ACL). Real 5-phase plan, Phase 1 scoped to a single call at a time (defers concurrency entirely). Registered as golden doc SIP-TWILIO-GATEWAY-NORTH. No code written -- planning only. (sess-20260902-2008-ed50169e)

## 2026-09-03 (7)
- feat: stdlib/idunapro/cli_mod.prn -- real second-pass upgrade for the `idunapro health` subcommand (cruise-queue card 9988's own next-named prerequisite), putting this session's own freshly-shipped `defenum` support (BURROW commit 8a3e64b) into REAL use for the first time outside its own unit tests. New `HealthError` defenum (`BadStatus`/`NotOk`, zero-payload variants) replaces `interpret-health-response`'s own bare `String` ErrorType. Real, honest v0 boundary carried over unchanged: BURROW's Go target still can't `match` on a user defenum's own variant, only Result/Option's fixed tags -- so the host (IDUNA_PRO's `cmd/idunapro/main.go`) reads the real, exported `.Tag` field directly to pick its own presentation message, the same real "PARENA decides WHAT happened, the host decides HOW to present it" split this file's own header already establishes. `exit-code-for-health` unchanged (still only needs Ok-vs-Err, not which variant). Verified via `burrow build` regenerating `IDUNA_PRO/internal/burrowgen/idunapro_cli_gen.go`, `go build`/`go test ./...` clean (zero regressions -- the existing generated tests only assert `.Tag`, never `.Value.(string)` for the Err case), and a real, live run against IDUNA's actual running `:8080` instance (healthy -> exit 0) plus an unreachable host (exit 1). (sess-20260902-2008-ed50169e)

## 2026-09-03 (6)
- 新增 `stdlib/net/wire.prn`(kanban 優先佇列卡片 PCAP-0022,回應 `NATIVE_PCAP_NORTHSTAR.md` 第 2 階段)。純 PARENA、無 FFI 的多位元組欄位擷取模組:`read-u16-be`/`read-u32-be`(big-endian 讀取)、`mac-to-string`/`ipv4-to-string`(欄位格式化),全部用既有原語(`bit-and`/`bit-or`/`shl`/`shr`/`char-at`)組成,沒有新增任何編譯器原語。真實找到並修正一個臭蟲:`char-at` 底層是 `(int32_t)s[i]`,此機器 `char` 為有號,位元組值 ≥128(絕大多數 MAC/IPv4 位址都有這種位元組,例如 192.168.1.1 的 192)會被符號延伸成負數——新增內部 `raw-byte` 輔助函式,用 `bit-and 255` 遮掉高位正確還原(-86 的 32 位元二補數是 `0xFFFFFFAA`,AND 上 `0x000000FF` 還是 `0x000000AA` = 170)。新增 `tests/test_wire.c`,故意包含 `0xaa`/`0xff`/`192` 等 ≥128 位元組的真實測試案例,不是只測小數值僥倖過關,6 個真實斷言全過。新增 `make test-wire` 目標。`make test`:345/345,無回歸。`STDLIB.md` 新增「net/wire」章節。(sess-20260902-2008-ed50169e)

## 2026-09-03 (5)
- feat: runtime/parena_runtime.h -- new `PARENA_NO_GRAPHICS` opt-out macro (kanban priority-queue card PARENA-0001, "dig into the parena OS implementation... smallest vertical slice... alpine? debian?"). Real, checked-not-assumed answer: Debian was already solved (this dev box IS a real Debian-family/glibc distro); Alpine's real, novel angle is musl libc. Built a real, no-root musl toolchain (`apt-get download musl musl-dev musl-tools` + `dpkg-deb -x` + a locally path-rewritten `musl-gcc.specs`, same trick class as this session's own libpcap-dev extraction). Real finding #1: `parena-c` (the compiler itself) already compiles clean and links fully static under musl, zero changes needed -- confirmed live building a real `.prn` file with the musl-built compiler binary. Real finding #2, the actual blocker: `parena_runtime.h` unconditionally `#include`d `<SDL2/SDL.h>`/`<SDL2/SDL_ttf.h>` for EVERY generated program, whether or not its own `.prn` source used `sdl2.prn` at all -- confirmed live via a real compile failure mixing this box's glibc-linked SDL2 headers with musl's own libc headers (`__gnuc_va_list`/`__time64_t` undefined, a real, structural glibc/musl header-mixing incompatibility, not a PARENA bug). Fixed by wrapping both SDL2 includes and the entire contiguous SDL2/SDL2_ttf function-implementation block in `#ifndef PARENA_NO_GRAPHICS`/`#endif` -- default (macro undefined) behavior is byte-for-byte unchanged, confirmed via the full local suite (345/345, zero regressions). Real, live, end-to-end proof: `stdlib/sip/message.prn` built with `-DPARENA_NO_GRAPHICS` under the musl toolchain produced a genuinely static ELF binary (`file`/`ldd` confirm `not a dynamic executable`, zero shared-library deps) that runs -- the real, concrete shape that will run unmodified on real Alpine. New `docs/OS_PORTABILITY_NORTHSTAR.md` with the full writeup, honest scope (doesn't prove a real SDL2-using program on musl -- needs Alpine's own musl-native `sdl2-dev`, not available in this sandbox; deliberately doesn't attempt a full custom-OS/distro-image build, that's the separate S213/FLASH thread), and not-yet-done follow-ups (no CI/Makefile wiring yet, this pass's proof was a manual one-off compile). Registered as golden doc `PARENA-OS-PORTABILITY-NORTH`. (sess-20260902-2008-ed50169e)

## 2026-09-03 (4)
- docs: real, phased plan for a native (no libpcap FFI) PARENA packet-capture implementation -- founder real-time follow-up on kanban card 435423: 'we want to build pcap into parena on a deep low level... a parena pcap implementation and build the std lib deps it needs into the std lib.' New docs/NATIVE_PCAP_NORTHSTAR.md. Real, checked-live finding worth surfacing: most of the real 'stdlib deps' this ask names already exist -- bit-and/bit-or/bit-xor/shl/shr real binops (added 2026-08-20 for compress/lz4.prn's own byte-level codec work), char-at as a real byte-buffer read -- confirmed live via a real compile that a big-endian 16-bit field read is already buildable today with zero new compiler work. The one genuinely missing, new capability: a raw AF_PACKET capture-socket runtime primitive, a real, honest, named Linux-only platform trade-off against libpcap's own real cross-platform BPF/Npcap/AF_PACKET abstraction. Real 4-phase plan: (1) the raw-socket runtime primitive (mirroring tcp_connect_impl's own already-established 'thin FFI over a raw syscall' shape), (2) a real net/wire.prn byte/field-extraction module built entirely from primitives that already exist, (3) native Ethernet/IPv4/TCP/UDP header parsing (pure PARENA, the direct structural sibling of this session's own sip/message.prn), (4) a real, PARENA-native filter (plain predicate functions on parsed structs, explicitly NOT full BPF bytecode compatibility -- named as real, separate, much larger, later work). Real, deliberate recommendation: keep this as an ADDITIONAL capability alongside the already-shipped FFI-bound pentest/pcap.prn, not a replacement -- libpcap's own real cross-platform portability and free BPF-filter-string compatibility are real, genuine value this native path doesn't have. No code written -- planning only, per the founder's own explicit 'plan that work' instruction. (sess-20260902-2008-ed50169e)
- feat: real host implementation for stdlib/pentest/pcap.prn (kanban priority-queue card 435423, 'parena PCAP primatives'). New tools/pentest_pcap_host.c: real pcap_open_live/pcap_next/pcap_compile+pcap_setfilter calls, a real, minimal, fixed-size opaque-handle table (Capture's own handle : I32 is a table index, never a raw host pointer), real safety choices (a real 1000ms read timeout, a real copy of captured bytes into the arena rather than a raw pointer into libpcap's own internal buffer -- a genuine use-after-free risk avoided). Real, live environment gap found and worked around, not blocked on: this box has libpcap's runtime installed but not the -dev package carrying pcap.h -- per this monorepo's own standing sudo-queue convention, queued (sudo-queue/47-install-libpcap-dev.sh) rather than installed directly; real, working, no-root alternative found and used for this pass's own live verification instead (apt-get download + dpkg-deb -x, no root needed at all, plus GNU ld's -l:libpcap.so.0.8 exact-filename linking against the already-installed runtime .so). Real, genuine build-integration gap found and fixed, distinct from the missing host code: ci_status.h's own -include forward-declaration trick can't reach a host function whose own parameter type is a struct the .prn compile itself generates (Capture) -- a -include'd header loads before parena_runtime.h even does. Real, working fix: sed splices the 3 real forward declarations in right after parena's own auto-generated prototype block, documented in the new test-pentest-pcap Makefile target. New tests/test_pentest_pcap.c: real, honest, environment-dependent assertions -- this sandbox has no CAP_NET_RAW/root, so start-capture against lo correctly reports a real Err, not a crash or fake success; a nonexistent interface always fails regardless of privilege. make test: 345/345, zero regressions. (sess-20260902-2008-ed50169e)
- feat: stdlib/sip/message.prn -- real SIP (RFC 3261) message parsing + construction (kanban priority-queue card 3124213, 'SIP phone primitives in parena stdlib'). Pure PARENA, no FFI needed: parse-message (real request/response parsing into a SipMessage struct + Vec of headers + body), build-request (a real, well-formed request with the standard mandatory header set), header-value (real O(n) lookup by name). Real, honest v0 boundary named explicitly: no SDP, no auth, no dialog/transaction state machine; real transport reuses the already-designed net/udp.prn, not duplicated. Two real, genuine compiler bugs found and fixed live, both halves of the same gap (SIP's own mandatory \r\n line ending was this stdlib's first-ever real trigger for a literal CR in a string): src/lexer.c's lex_string() had no \r case at all (decoded to a bare 'r' byte, no signal); src/emit.c's own string re-escaping loop had the matching gap on the way back out (a raw CR byte embedded verbatim into generated C, a real invalid-syntax error). Both fixed. A third, real, separate emitter quirk found and worked around, not fixed: a match clause's own Ok-bound STRUCT payload is always declared void* in generated C regardless of its tracked type -- get-field directly on it fails; deref is the real, already-established path that correctly casts it back, confirmed via a minimal isolated repro before applying here. 2 new tests/test_lexer_parser.c + 3 new tests/test_emit.c assertions for the \r fixes. New make test-sip-message target: 9/9 real end-to-end assertions (INVITE parse, 200 OK parse, header hit/miss, malformed-input Err, a built request round-tripping back through the parser). make test: 345/345, zero regressions. (sess-20260902-2008-ed50169e)
- docs: ANDROID_JAVA_TARGET_NORTHSTAR.md moved to a new real upstream repo, SPIDERBEETLE (founder created it 2026-09-03 for kanban cruise-queue card 32445324), same real precedent BURROW's own DUNG.md set when DUNG got its own repo. PARENA/docs/ANDROID_JAVA_TARGET_NORTHSTAR.md kept as a short pointer (not deleted, so existing STDLIB.md/golden-docs-index.md references don't dead-end). stdlib/android/battery_ui.prn stays here (the reusable .prn source); the compiled BatteryUi.java + smoke test + any future real Android app code now live in SPIDERBEETLE itself. STDLIB.md's own android/battery-ui section updated to point to the new real location. (sess-20260902-2008-ed50169e)
- feat: real Java-target proof for a future PARENA Android app (kanban cruise-queue card 32445324, 'PARENA android app in JAVA using PARENA using the JAVA emitter'), plus a real fix: (not x) had NO handling anywhere in src/emit_java.c -- the exact same real gap already found and fixed in src/emit.c (2026-08-21) and BURROW's own emit_c.go/emit_go.go (2026-08-30), this Java target simply hadn't hit a real .prn file using it yet, fixed via Java's own ! operator, same real port. New stdlib/android/battery_ui.prn: two real, minimal scalar decision functions (should-show-low-battery-warning, clamp-brightness) plausible for a real Android app (MJOLNIR is this monorepo's own real precedent for what one actually needs). Compiled via the real Java emitter and verified with an actual JDK (javac/java 25.0.4, same version STDLIB.md's own BezierInterp/Humanness three-target proof already used) against a real Main.java smoke test -- 7 real assertions, all pass, including the real battery-pct==15 boundary and both brightness-clamp directions. New docs/ANDROID_JAVA_TARGET_NORTHSTAR.md: honest, checked-not-assumed scoping for the real remaining gap -- this sandbox has no Android SDK at all (no ANDROID_HOME/sdkmanager/adb), and MJOLNIR itself (this monorepo's own real Android app) has no gradlew wrapper checked out locally either, confirming it's never actually built here, only via real CI/the founder's own machine -- so a real, buildable APK is honestly named as NOT attempted rather than guessed-at unverified scaffolding. Real, concrete next steps named (Gradle skeleton, wiring into a real Activity/BatteryManager callback, a real CI step with the Android SDK), none started. New tests/test_emit_java.c coverage: 4 new assertions for the not fix. tests/test_emit_java: 31/31. make test: 342/342, zero regressions. (sess-20260902-2008-ed50169e)
- feat: stdlib/v16/lexer.prn -- real, first-slice JS tokenizer for the V16 JS engine (kanban priority-queue card 34134124, 'parena v16 iteratejs engine'), Phase 2a per docs/V16_NORTHSTAR.md's own real phased plan. Real Phase 1 decision made and checked live first: VS0 still has no generic type parameters (a bare (Vec T)/(Map K V) signature still fails), but (Vec ConcreteStruct) compiles fine -- JsToken is one concrete, non-generic struct, the narrowest real form of the doc's own 'commit to concrete types' path. lex walks a source string once producing a (Vec JsToken) ending in a real TokEof sentinel. Real, honest v0 scope named in the file's own header comment: integer-only numbers, no string escape decoding, identifiers with no real keyword table yet, a small fixed single-character punctuation set, no comments/regex/template strings -- an unrecognized byte is silently skipped, not a crash. Real, live bug found and fixed during this pass, not an emitter bug: an early single-file 'parena build stdlib/v16/lexer.prn' (without stdlib/string.prn combined into the same invocation) produced C with real, serious type-confusion warnings (void *n = length(src); double i = 0;) -- root cause confirmed via a minimal 2-line repro: VS0's multi-file build has no real module-linking step, a called-but-not-combined function's return type silently falls back to a wrong void* guess instead of erroring. Fixed by combining stdlib/string.prn + stdlib/v16/lexer.prn in the same build invocation (matching test-bstree's own already-established real precedent), documented explicitly in STDLIB.md so the next .prn file calling into another module doesn't lose time rediscovering this. New Makefile target test-v16-lexer + tests/test_v16_lexer.c (4 real end-to-end assertions: a real mixed-token source, an empty source, an unterminated string's own honest degenerate handling, an unrecognized-byte skip) -- all pass. make test: 342/342, zero regressions (this file doesn't touch the compiler itself). docs/V16_NORTHSTAR.md and STDLIB.md updated with the full real writeup. (sess-20260902-2008-ed50169e)
- docs: PARENA/docs/V16_NORTHSTAR.md -- real, scoping-only NORTHSTAR pass for a native-PARENA JavaScript engine (kanban card S189-63, founder-named 'V16,' explicitly called 'table stakes'). Honest size comparison to building V8/Node.js from scratch; hardest real blocker named directly: VS0 has no generic type parameters today, confirmed live again, blocking a real JS value representation, a general object model, and GC-vs-region-allocation strategy for JS's own closures/circular refs. Real 6-phase plan, explicit out-of-scope list (JIT, full spec compliance, DOM, npm). No implementation code, planning only. (sess-20260902-2008-ed50169e)
- feat: stdlib/idunapro/cli_mod.prn -- real match/Result decision logic for kanban card 9988's own CLI, compiled via BURROW's Go target into IDUNA_PRO/internal/burrowgen and driven by a new real Go host binary (IDUNA_PRO/cmd/idunapro), live-verified end to end against IDUNA's running :8080 instance (health check success + unreachable-host failure, both real exit codes). See STDLIB.md 'idunapro/cli-mod' section for full writeup. (sess-20260902-2008-ed50169e)
- 新增 `stdlib/papercraft/explosion_mod.prn`(kanban priority queue 卡片「499988:can we start to
  add more api surfaces to PAPERCRAFT mod api surface we want to get parity with ROBLOX apis
  start with the api for setting an explosion」)——真正的線性爆炸傷害衰減,對照的是 Roblox 自己
  真實的 `Explosion` 物件(`Position`/`BlastRadius`)這個形狀,這次先做傷害的部分,
  `BlastPressure` 那種真正的推力/擊退效果留給下一個、獨立的 API surface,沒有一起做。跟這個
  repo 目前每一個 PAPERCRAFT mod 一樣的真實 I32-only、「mod 決定、host 套用」分工。已經在
  PAPERCRAFT 那邊真的用 `parena build` 編過、`gcc -Wall -Wextra` 乾淨、真的寫了測試全部通過,
  也真的用 `bazel test //packages/simulation:all`(15 條測試)跑過,零回歸。誠實記下真實、還沒
  接線的缺口:PAPERCRAFT 目前完全沒有真正會觸發爆炸的 host 端遊戲邏輯(跟 `level_mod.prn` 自己
  文件裡明講的「套用 XP 的 host 端還沒寫」是同一種真實缺口)——這是真的、測試過的決策邏輯,還
  不是真正在遊戲裡能用的功能。 (sess-20260902-2008-ed50169e)

## 2026-09-03 (3)
- 新增 `stdlib/bstree.prn`(kanban priority queue 卡片「9933:INDEXING primitives built into
  PARENA to power IDUNA OG unified search - btries etc」的真實答案):一個真正、會編譯、有測試
  (`make test-bstree`)的 String 鍵、I32 值二元搜尋樹——insert(真正的 insert-or-update,不是
  insert-only)、get(`Option I32`)、`contains?`。兩個開工前就真的查證過的決定性設計限制:(1)
  VS0 emitter 目前完全不支援泛型型別參數——`vec.prn` 自己的 `(Vec T)` 跟 `map.prn` 自己的
  `(Map K V)` 現在都真的編不過(「unsupported return type symbol」,現場驗證過不是假設),所以
  `bstree.prn` 直接鎖定具體型別 String/I32,跟 `json.prn` 自己的 `JObject` 為了同一個理由已經用
  過的解法一樣。(2)節點全部放在同一個扁平的 `(Vec BSTNode)` 裡,`left`/`right` 是整數索引,不是
  真正的指標——跟 `map.prn` 自己「扁平陣列、不用指標鏈」的設計一樣,順便完全不用去搞清楚 VS0
  到底支不支援自我參照的 struct 型別(根本不需要知道答案)。真實、誠實的範圍:這是一顆普通、不
  平衡的 BST——平均真的是 O(log n),但如果照排序好的順序插入,最壞情況真的是 O(n),沒有旋轉/
  重新平衡。真正的多路、磁碟導向 B-tree(卡片字面上寫的「btries」)是真實、更大、獨立的後續工作,
  這次沒有做。把這個接進 IDUNA 自己的統一日誌搜尋(`internal/http/handlers/logs.go`,現在是真的
  線性 `Scan`)也是另一件獨立、這次沒動手的工作——IDUNA 是 Go host,那個整合需要 `BURROW` 的原生
  Go emit target,跟 IDUNA_PRO 自己的 extensibility 計畫已經確立的理由一樣。過程中真的找到並繞開
  (沒有去修)兩個 VS0 emitter 的真實怪癖,寫在檔案自己的 header comment 裡:`cond` 非第一個
  clause 的 body 裡面直接包 `let` 會報一個真實、誠實的「不能用在 expression position」錯誤,即使
  同樣的 `let` 包在 `if` 分支裡在這個 session 別的地方明明編得過;還有
  `(get-field (vec/get &v i) :field)` 一定要先用 `let` 把 `vec/get` 的結果綁定起來,不然產生的
  C 會對一個沒轉型的 `void *` 直接解參照。`make test`(342 條核心語言測試)、`test-process`、
  `test-mixforge-import` 全部綠燈,零回歸。(sess-20260902-2008-ed50169e)

## 2026-09-03 (2)
- 新增 `stdlib/mixforge/import.prn`(S243-01,MIXFORGE 的 V0:貼一個 YouTube 網址——加一個可選的
  第二個網址帶伴奏版——真的落地成本地音樂庫)。真實下載機制:透過 `process/run-capture` 呼叫真正、
  未修改的 `yt-dlp` 二進位檔,`--print after_move:filepath` 拿回後製完之後真正的最終路徑,不用自己
  猜檔名/副檔名。**真實、明確處理的安全議題**:`run-capture` 會把整個指令丟進 `/bin/sh -c`,是真正的
  shell injection 風險——`safe-youtube-url?` 是一個真正、狹窄的白名單(只接受 youtube.com/youtu.be
  兩個 host,而且整條網址逐字元檢查,只准英數字加上一組固定、真正 URL 會用到的標點),重用
  `log/projector.prn` 已經驗證過的 `shell-single-quote` 再包一層。metadata 誠實地寫成 NDJSON 一行
  (不是真的 SQLite——`sql/driver` 在 PARENA 目前只是設計、還沒實作,直接 FFI-bind libsqlite3 是另一個
  獨立、還沒開始的工作,在檔案自己的 header comment 裡明確記下來)。新增
  `tests/test_mixforge_import.c`(`make test-mixforge-import`):真實驗證白名單擋掉每一種真正的
  shell injection 手法(`'; rm -rf /`、反引號、`&&`、`|`、`$HOME`)、真實用暫時 PATH 目錄假冒
  `yt-dlp`(跟 `test-log-projector` 驗證 sqlite3/mysql 同一招)驗證真正的指令組成方式跟真正的
  Ok/Err 傳遞,含 import-track 有無伴奏版兩種情況都測到。`./parena build` 一次過、`gcc -Wall -Wextra
  -pedantic -Werror` 乾淨、`make test`(342 條核心語言測試)跟 `make test-process`/
  `test-log-projector`(相鄰、共用 process/log-projector 模組的既有測試)全部綠燈,零回歸。
  (sess-20260902-2008-ed50169e)

## 2026-09-03
- STDLIB.md 新增一段真實調查記錄(創辦人,kanban cruise queue:「emojis need to work in pitviper and parena editor what do we need to build a custom emoji font or use image files or something?」)。真實、查證過的答案:不用另外做自訂字型或圖片檔——`sdl2.prn` 自己的 `open-font` 本來就是完全通用的(任何真實 TTF/OTF 路徑),載入 Noto Color Emoji(已確認系統上真的有裝)當第二個真正的 `Font` handle,完全不用新增任何 FFI,跟 PITVIPER 自己 Go 那邊已經驗證過能動的做法一模一樣。真實、誠實找到的另一個、更根本的前置缺口,沒有含糊帶過:`editor/render.prn` 的 `render-tokens` 一次只用一個 `Font` 畫完整個 token span;要真的混合 emoji 字型,需要先偵測 span 裡面哪些字元是 emoji 範圍的 codepoint 才能分段——但 `string.prn` 自己的 `char-at` 就是明講「回傳原始 byte 值」,完全不懂 UTF-8,整個 stdlib 目前沒有任何一個函式真的會把 UTF-8 位元組序列解碼成真正的 Unicode codepoint。這一步(真正的 UTF-8 解碼)是比字型載入更根本、需要獨立規劃的真實缺口,這次沒有動手做,只是誠實記錄下來,讓之後真正的解法(重用 PITVIPER 已經驗證過的字型設計,等 codepoint 解碼做出來之後)不會被忘記。(sess-20260902-2008-ed50169e)

## 2026-09-02
- 自舉編譯器真正的第六步(founder real-time: "continue working on parena self hosted compiler"):新增真正、獨立、會解析 argv 的 `parena-selfhost` binary,補上 `selfhost/main.prn` 自己文件裡早就誠實寫明的那個缺口——「還不是一支真正的、會解析 argv 的獨立執行檔」。沒有另外教 `parena-c` 一個新的「PARENA `main` → C `int main`」emission 慣例(那是更大、更獨立的一塊,還沒開始),而是延續每一支 selfhost 測試 driver 早就用過的同一套真實做法:一支小小的、手寫的 C `main`(`selfhost/cli_main.c`)做真正的 OS 介接(argv 轉成真正的 `Vec String`,回傳真正的 exit code),直接呼叫編譯好的 `build-file`/`build-files`——一樣真正對照 `src/main.c` 自己的 `build` 子命令格式(`parena-selfhost build <files...> -o <out.c>`)。新增 `make parena-selfhost`/`make test-selfhost-cli`(後者是真正 fork+exec 編譯好的 binary 本身,不是在同一個 process 裡呼叫 build-file/build-files)。

  真正拿來用的第一件事,就抓到一個真實、活生生的 segfault:用新 binary 真的嘗試「自舉編譯」——把 `parena-selfhost` 指向自舉編譯器自己的真實原始碼。`selfhost/lexer.prn`/`parser.prn` 跟三個 stdlib 依賴都編譯正常;`selfhost/region.prn`/`emit.prn`/`main.prn` 各自獨立都真的 segfault。用 `gdb`(重編一份帶 `-g -O0`)直接指到 `match-pattern-payload-name` 自己的 `(get-field payload :text)`:`match-pattern-has-payload?` 原本只檢查 pattern 的 kind(是不是 call-shaped),沒檢查它是不是真的有 payload 這個 child——一個真實、零 payload 的 variant pattern 用括號寫出來(`((None) body...)`,`selfhost/region.prn` 自己一堆 match clause 裡到處都是這種真實寫法)一樣是 call-shaped,但只有一個 child,所以 `match-pattern-payload-name` 自己那個沒有條件檢查、直接讀 `children[1]` 的寫法,是一個真實、會爆的越界 `Vec` 存取。修法通用、不是針對 `None` 這個名字特別處理:改成同時檢查 child 數量 >= 2。修完之後,每一個原本會 segfault 的檔案,還有完整、真實的 8 檔自舉編譯嘗試(`stdlib/string.prn`+`array.prn`+`io.prn` 加上全部 5 個 selfhost domain)全部乾淨跑完,exit 0,真的輸出出真正的 C。新增回歸測試(`tests/test_selfhost_emit.c` 新案例 + 新的 `tests/integration/driver_none_paren_pattern.c`):同一個原本會爆的 pattern 形狀,結構檢查(沒有 `#error`,而且——這次的重點——沒有爆掉)加上真正編譯執行的斷言。

  誠實記下這次真正發現、還沒動手做的新邊界:自舉編譯出來的真實輸出(`self_compiled.c`,1544 行真的 C)本身還不能被 `gcc` 乾淨編過——`selfhost/emit.prn` 自己真正的 emitter 目前完全沒有支援 `#target`/`inline-c` 這種 FFI body(`stdlib/string.prn` 自己的 `length`/`char-at` 等等全部都是這樣寫的——有查證過,不是猜的:真正的 `parena-c` 編譯器今天可以正確處理這些,所以這是自舉 emitter 自己真實、獨立的範圍缺口,不是正式編譯器的任何退步)。真正的完整自舉(自舉編譯器真的把自己編成一支能動的 binary)還需要 `selfhost/emit.prn` 補上真正的 `#target` emission 支援——真實、不小的一塊,這次沒有動手做。這次本機 `bazel build //...` 沒辦法驗證(這個 checkout 本身既有、只在本機發生的權限問題:殘留的 `bazel-*` convenience symlink 指到另一個使用者帳號自己的 cache——真實的環境怪癖,CI 自己乾淨的 runner 不會踩到這個),但完整的 Makefile 全套測試(342 個)跟全部 `test-selfhost-*` 目標都是乾淨的。

- 真正修好上面那個 CRLF bug,而且真的追出並修好第二個、更深一層、真正擋住整條 webdriver 鏈的 bug(founder real-time: "fix")。CRLF 修法:`net/http.prn` 的 `build-request` 改用 `(string/concat (string/char-from-code 13 dest) "\n" dest)` 真的組出 `\r\n`,取代原本全部裸 `\n` 的寫法——真的用一個獨立、繞過 PARENA 自己 TCP client 的 raw socket 探針(直接呼叫 `tcp_connect_impl`/`tcp_write_impl`)截下真正送出去的位元組,確認組出來的 request 逐位元組正確(真的是 `\r\n`,不是 `\n`)。改完之後 `make test-webdriver` 對假伺服器還是全綠,但真的對著 chromedriver 跑會整個卡死——沒有 timeout,真的永遠 hang。追出真正原因,也是同一支獨立探針抓到的:chromedriver 自己的回應雖然真的有帶 `Connection:close` 這個 header,但真的量過,它並沒有真的把 TCP 連線關掉(用一個一模一樣的 request 直接透過 bash `/dev/tcp` 手動測過兩次,`cat` 都卡住等不到 EOF,不是猜的)。`net/tcp.prn` 的 `tcp-read`(`tcp_read_impl`)原本的設計就是「一直讀到對方關閉連線為止」——這個假設對 IDUNA 自己的 Go `net/http`(真的會關閉)成立,對 chromedriver(不會真的關閉,即使它自己回應裡寫了 close)完全不成立,所以之前每一次真正對 chromedriver 送出的 request 理論上都會永遠卡住,不是這次才踩到,是這條路徑本來就有的、文件自己早就誠實寫明「no Content-Length-aware early stop... a real future gap」的那個真實限制。真正的修法:`tcp_read_impl` 現在會邊讀邊找 `\r\n\r\n` 這個 header/body 分界,一旦找到就在裡面找真正的 `Content-Length` header(大小寫不分),知道之後就只讀到剛好那個位元組數為止,不用等對方關閉;真的沒有 `Content-Length`(chunked 或純靠關閉分界的回應)的情況,行為完全不變,還是照舊讀到 EOF——新增一個真正的 30 秒 idle timeout(用 `poll`)當作最後一道防線,不會再無限期卡死。這是 `net/tcp.prn` 這個 runtime 層級的真正修法,不是只修 webdriver 專用的路徑——所有真正用到 `tcp-read` 的呼叫端都吃得到這個修法。真正端到端驗證過,不只是跑既有測試:重新編譯 `/tmp/webdriver_fetch_real`(獨立的、真正呼叫編譯出來的 `new_session_with_capabilities`/`navigate_to`/`page_source`/`quit_session` 的 C harness),對著真正、剛裝好的 chromedriver + 真的 Chrome for Testing 打 `https://example.com`,真的秒回、真的拿到正確、逐字元乾淨的 render 完 HTML(`<html lang="en">...`),不是假伺服器、不是猜的。`make test-webdriver` 跟 `make test-http-router` 都還是全綠(後者額外用 `-Werror -pedantic` 編過,runtime 改動沒有引入新警告)。

- 真正裝好、真正跑過一次 chromedriver + 真的 Chrome(founder real-time: "yea set up the webdriver")。用 `npx @puppeteer/browsers install`(不用 sudo,裝在 `~/.local/browsers`)裝了真正版本對齊的 `chrome@stable`/`chromedriver@stable`,對著真正跑起來的 chromedriver 用 curl 直接發真正的 W3C `/session` request,真的成功建立 session、navigate 到 `https://example.com`、`GET .../source` 拿到真正 render 完的 HTML——證實 chromedriver+Chrome 本身在這個 sandbox 真的能動(headless、`--no-sandbox`)。過程中修好一個真實、live 抓到的 bug:先前那版 `host_json_unescape`(`tests/test_webdriver.c`)把 `\uXXXX` 的四個十六進位字元原封不動複製過去(`<` 變成字面上的 5 個字元 "003C",不是真正的 `<` 這一個字元)——這個 bug 之前完全沒被抓到是因為假伺服器(`tests/fake_webdriver_server.go`)先前被改成不做 HTML escape,直接繞過了整個問題;真的對著 chromedriver 跑過才確認:真正的 driver 確實會把 page-source 回應裡的 `<`/`>` escape 成 `<`/`>`(跟 Go/JS 的 JSON encoder 預設行為一樣),所以假伺服器原本那個「取消 escape」的修法方向根本是反的——已經改回真正會 escape,真正的修法搬到 `host_json_unescape` 自己身上,補上正確的 BMP `\uXXXX`→UTF-8 解碼(surrogate pair 之外的完整範圍都對,已知、誠實聲明的限制:U+FFFF 以上的 astral-plane 字元的 surrogate pair 還沒處理,但 HTML escape 用到的 `<`/`>`/`&`/`"` 全都在這個範圍內)。新增 `new-session-with-capabilities`(真正需要用到:空的 `alwaysMatch` 讓 chromedriver 用自己的預設值開瀏覽器,在沒有 X display、沒有真正 sandbox 權限的容器裡會直接啟動失敗——"session not created: Chrome instance exited",真的用真正的 chromedriver 對出這個真實錯誤才發現,不是猜的;新函式讓呼叫端可以塞自己的 `goog:chromeOptions` 之類的真正 capabilities JSON,例如 `--headless=new --no-sandbox --disable-gpu --disable-dev-shm-usage`)。`make test-webdriver` 全綠(0 failures,新增 `page-source`跟`new-session-with-capabilities`各自的斷言)。

  誠實記下這次真正發現、但這次沒有動手修的更深一層問題:拿掉假伺服器、真的對著 chromedriver 用同一支編譯出來的 client(`new-session-with-capabilities`)發 request 時,真正的 chromedriver 回一個空 body 的 4xx/5xx(`ProtocolError`,message 是空字串)——但用 curl 送一模一樣的 JSON body 給同一個 chromedriver 卻是真的成功。追出真正原因:`net/http.prn` 的 `build-request` 自己的文件已經誠實寫明,整段 request 只用裸的 `\n` 當換行(不是真正 HTTP/1.1 規定的 `\r\n`),理由是 PARENA 自己的字串字面值 lexer 目前不支援 `\r` 這個逃脫字元;文件裡也已經寫明這是「靠寬鬆的伺服器擋著」的權宜做法,先前只驗證過對 IDUNA 自己的 Go `net/http`(本來就對裸 LF 寬容)。chromedriver 自己內建的 HTTP server 顯然不夠寬容——這正是這次真正被抓到、之前完全沒人踩過的真實後果。真正的修法(在 `build-request` 用 `string/char-from-code 13` 組出真正的 CR 字元,插進每個換行的地方)是可行的,但牽涉到重寫這個函式目前深度巢狀、逐字面值硬編碼換行符的字串組裝寫法,風險跟範圍已經超出這次「把 webdriver 設定起來」的範圍,誠實記下來、留給下一次專門處理,沒有硬幹著趕在這次做完。

- `stdlib/net/webdriver.prn` 新增 `page-source`(`GET /session/{id}/source`,回傳目前真正、已經 render 完的整頁 HTML)。動機是真實的:PRRJECT_FATBABY 這次想幫 BusinessWire 加一個跟 PRNewswire 一樣的 press-release scraper,但創辦人親自用瀏覽器確認過 BusinessWire 的新聞列表頁是純 client-side JS render、原始 HTML 完全沒有資料,既有的「plain HTTP GET + regex」scraper 架構在這種頁面上天生就不會動;founder real-time 接著想起「我們架構裡某個地方好像已經有 selenium bindings」——查證後確認就是這個檔案自己(`net/webdriver.prn`,真正的 W3C WebDriver 協定 client),之前只做到 `new-session`/`navigate-to`/`find-element`/`element-click`/`element-text`/`page-title`,沒有任何一個函式能把整頁 render 完的 HTML 拿出來重新跑既有的 regex 抽取邏輯,`page-source` 補的正是這一塊。過程中真的抓到並修好一個活的 bug:`tests/fake_webdriver_server.go` 用 Go 標準 `encoding/json` 內建的 `Encoder`,預設會把 `<`/`>`/`&` 轉成 `<` 這類 unicode escape——真正的 chromedriver/geckodriver 完全不會這樣做(一個真正的 page-source 回應裡全是原始的 `<`/`>` HTML tag 位元組),這個預設值一開始讓 `page-source` 自己的新測試直接 FAIL,不是 PARENA 這邊的 JSON parser 有問題;修法是 `enc.SetEscapeHTML(false)`,讓假伺服器的行為對齊真正的驅動程式,不是繞過測試。新增 `tests/test_webdriver.c` 的 `page-source` 端到端斷言(對著 `fake_webdriver_server` 真的跑過)。`make test-webdriver` 全綠(0 failures)。誠實聲明:真正串到 PRRJECT_FATBABY 的 Go pipeline(讓 prwatch 呼叫一個編譯好的 PARENA 小程式做「開瀏覽器 session → navigate → page-source → 印到 stdout」)還沒做,而且這個 sandbox 沒有裝 chromium/chromedriver,BusinessWire 真正的頁面結構也還沒人親眼看過——這次只確定了「拿到 render 完的 HTML」這一步本身是真的、測試過的,不是整條 pipeline 都做完了。創辦人接著提到可以參考 MISHRI 自己已經真正存在的 humanness 層(`stdlib/mishri/humanness.prn`/`bezier_interp.prn`,`chance`/`random-int`/`gaussian-noise`/`bezier-interp`,都是純函式、C target 應該一樣能編)幫 webdriver 的請求加一點人類感的節奏/延遲,甚至提過讓 scraper 偶爾點錯東西、在網站上晃一晃——後者創辦人自己隨後表態「不確定/先別做」,這次就照做暫停,沒有加任何主動偽裝/規避偵測行為;純計時抖動(reuse 既有的 `chance`/`gaussian-noise` 而不是新發明一套)如果之後真的要做,是一個小、獨立、風險低很多的加法,留給明確決定要做的時候再做。(sess-20260830-1207-cc0ba7da)

- Controllers 真的做出來了(S225-04):新增 `stdlib/http/controller.prn`(`Request`/`Response` + `response-ok`/`response-json`/`response-not-found`)跟 `examples/shithub_controller_demo.prn`(真正、明確標示是示範用的 `demo-repos-index`/`demo-repos-show` 兩個 action,加上 `dispatch`——`match-route` 配對到哪條路由,一段真正手寫的 `if`/`string/str-eq?` 分派鏈呼叫對的 action)。`Request` 誠實決定不放通用 params 集合,跟 `log/event.prn` 的 `fields-json` 同一個理由(整個 stdlib 沒有真正驗證過能動的 Vec-of-tuple)。`http/routes.prn` 新增 export `vec-route-at` 讓外部可以讀出配對到的路由自己的欄位。誠實、沒變的限制:分派邏輯沒辦法做成通用 stdlib 函式(PARENA 的 `fn` 不能捕捉外部變數,跨模組一等函式參照也還沒確認能用),每個真正的應用程式都要自己寫一次。新增 `tests/test_http_controller.c` 端到端驗證(真正的 Router、真正的 Request 走完整 pipeline、對的 action、真正的 404)。既有全套測試不受影響(342 全過)。commit `1e34dc4`。(sess-20260830-1207-cc0ba7da)
- 新增 JSONL event log + SQL projectors(SQLite/MySQL/PostgreSQL)(founder real-time: "continue building the framework with jsonl log streaming with mysql psql sqlite etc projectors")。LO FRAMEWORK_NORTHSTAR.md 自己 event-sourcing 這條延伸線的真正落地:新增 `stdlib/log/event.prn`(`Event` struct + `event-encode`——`json.prn` 自己既有真正的 parser 從來沒有對應的編碼器,這次補上一個窄的、只給這個用途用的版本;誠實決定 `fields-json` 是一個普通、已經組好的 JSON 字串,不是 `(Vec (String String))`——直接查證過,整個 stdlib 完全沒有任何真正、驗證過能動的 Vec-of-tuple 用法,`tokenizer.prn` 自己的 `merges` 欄位宣告了但從來沒被讀寫過,`regex/pcre.prn` 自己的 `Match.groups` 同樣的形狀也被文件自己標記成永遠是空的)、`stdlib/log/jsonl.prn`(`append-event!`/`read-lines`,真正的檔案 I/O)、`stdlib/log/projector.prn`(`events-table-ddl`/`insert-event-sql`——三種資料庫共用同一張通用的 `events` 表,`fields` 存成原始 JSON blob,每個 kind 各自型別化的表格留給之後——加上 `project-sqlite!`/`project-mysql!`/`project-postgres!`,各自透過新增的 `stdlib/process.prn` `run-capture`/`run-capture-exit-code`(真正、通用的 popen/pclose 子行程執行+輸出擷取,補上那個檔案自己早就點名的「real, separate, future scope: captured stdout/stderr」缺口)呼叫真正的 CLI 客戶端)。過程中抓到兩個真實、之前完全沒被踩過的 compiler 缺口:(1)`match` 綁定出來的 `FileHandle`(例如 `(Ok !f)`)在 `.prn` 層級看起來是真正的 `FileHandle`,但實際產生的 C 還是 Result 自己那個通用的 `void*`,直接傳給宣告 `FileHandle` 參數的函式會編不過——整個 stdlib 之前完全沒有真正的 `io/file-open` 呼叫端,新增 `unbox-filehandle` 解決,跟 `regex/pcre.prn` 自己的 `unbox-bool` 同一類修法;(2)`OpenMode` 這種零欄位 defenum 的建構呼叫必須寫成裸符號(`Append`),不能加括號寫成 `(Append)`——`emit_expr` 裡零欄位變體的辨識邏輯只認 `NODE_SYMBOL`,不認括號包起來的零參數呼叫,寫成 `(Append)` 會被誤判成呼叫一個不存在的 `Append()` 函式。誠實聲明目前的真實狀態:這個 sandbox 沒有裝 `sqlite3`/`psql` CLI,也沒有可用的 MySQL 帳密(直接查證過,不是猜的——`sudo-queue/NOT_INCLUDED.md` 自己早就把 MySQL 帳密列成 S30-02 這個既有缺口),所以每個函式都只驗證過自己產生的 SQL 文字跟真正的 shell 呼叫管線(用一個放在 PATH 上的假 CLI script 頂替),還沒對著真正、活著的資料庫跑過。新增 `sudo-queue/45-install-sqlite3-and-postgresql-client.sh` 排進去,是真正、下一步要做的事。新增 4 個 Makefile 測試目標(`test-process`/`test-log-jsonl`/`test-log-projector`),全部都是真正跑過的端到端測試(真正的檔案 I/O、真正的 JSON 來回轉換、真正的子行程執行+擷取+exit code、真正的 shell 呼叫透過假 CLI 驗證)。既有全套測試不受影響(342 全過,`test-http-router`/`routes`/`webdriver` 依然全綠)。commit `0569617`。(sess-20260830-1207-cc0ba7da)
- 修好一個真實的 shell 引號 bug,是創辦人真的跑了 `sudo-queue/45-install-sqlite3-and-postgresql-client.sh` 之後,對著真正的 `sqlite3` CLI 活生生測出來的:`run-sql-via` 原本把整段 SQL 文字直接包在普通雙引號 `"..."` 裡面丟給 shell,但 SQL 文字本身可能含有雙引號(來自 JSON `fields-json` 的值)——shell 自己的雙引號解析會在第一個內嵌的 `"` 就提早結束,後面的內容全部被當成沒加引號的字重新斷詞。真實驗證過的後果:`{"name":"PARENA"}` 存進真正的資料庫變成 `{name:PARENA}`,雙引號整個消失,不是單純轉義錯誤,是真正的靜默資料損壞。修法:新增 `shell-single-quote`(標準 POSIX 單引號跳脫:整段包在 `'...'` 裡,內嵌的 `'` 換成 `'\''`),在 `run-sql-via` 裡對完整 SQL 文字套用一次——單引號不會被 `$`/反引號/反斜線展開,也不會被內嵌的雙引號提前結束,JSON 這種內容因此安全。`sql-escape-string` 自己的 `'`→`''` 加倍還是分開、獨立需要的一層(那是 SQL 自己字串常值的跳脫規則,不是 shell 的),兩層現在正確疊在一起,不再只套用其中一層。新增一個真正、永久、環境相依會自動跳過的即時測試(`test_log_projector.c`):真的寫一個帶 JSON fields 的 Event 進 `project-sqlite!`,對著真正的 `sqlite3` CLI 讀回來,確認存的 JSON 逐字元正確——這正是如果當初寫這個檔案時 `sqlite3` 就已經裝好,原本就該抓到這個 bug 的那個斷言。既有全套測試不受影響(342 全過)。commit `d675f86`。(sess-20260830-1207-cc0ba7da)
- 新增 stdlib/http/routes.prn:LO FRAMEWORK_NORTHSTAR.md 自己 Rails 風格「batteries included」計畫的真正落地(founder real-time: "ok lets build SHITHUB using LO... define the api first of the web framework... make it like rails")。既有的 `http/router.prn` 已經有真正的路徑「樣式」比對(`route-matches?`/`extract-param`);這次補上 Rails 自己 `config/routes.rb` 才有的 HTTP method + route table 這一層:`Route`(method/pattern/handler-name)、`add-route!`(對應 Rails 自己 `get "/repos", to: "repos#index"` 的宣告寫法)、`match-route`(照宣告順序找第一個 method+path 都吻合的路由,找不到回傳誠實的 -1,不是 Option)、`resource-routes`(對應 Rails 自己 `resources :repos` 一行產生全部 5 條標準 RESTful 路由:index/create/show/update/destroy)。誠實聲明的範圍限制:`handler-name` 目前只是普通字串,不是真正可以呼叫的函式值——PARENA 自己的 `fn` 字面值不能捕捉外部變數、只能是檔案層級的 C 函式,跨模組的一等函式參照目前也還沒確認能用,所以 route table 沒辦法真的「呼叫」自己配對到的 handler;`match-route` 只負責告訴呼叫端配對到哪一條路由,實際要呼叫哪個 action 由應用程式自己的 dispatch 邏輯(現在是手寫的 `cond`/`match`,以後 LO 的 qi 做出來就能真的用具名函式呼叫)決定。過程中抓到並繞過兩個真實的 compiler 限制:struct 字面值不能直接當函式呼叫的參數(要先用 `let` 綁起來,跟 S222-09 自己踩過的 MatchBudget 是同一類問題);把一個真正的 struct 值(不是純量)push 進 Vec 需要一個真正的 Arena 才能 box,補上 `dest` 參數解決。新增 `tests/test_http_routes.c`:路由宣告順序、method+path 配對、真實的「找不到」情況、`resource-routes` 產生的全部 5 條路由都驗證過。`make test-http-routes` 全綠、`-Werror -pedantic` 乾淨,既有全套測試不受影響(342 全過,`test-http-router` 依然全綠)。commit `52b2d92`。(sess-20260830-1207-cc0ba7da)
- 修好一個真實的 compiler 錯誤訊息問題(EMILY/BACKLOG.md SECTION 223 / S223-01):`let`/`match`/`loop` 直接拿來當 `if` 的條件式(表達式位置)用,原本會報一個完全誤導的「unknown identifier '<綁定的名字>'」,而不是講出真正的問題。LO 自己這週的 S222-09(把 MATCH 接成真正跑得動的 regex/pcre 呼叫)過程中真的踩到並確認這個 bug——不是 LO emitter 自己的問題。根本原因:`emit_expr`(`if` 自己的 cond/then/else 都是走這個純表達式 dispatcher)對 `let`/`match`/`loop` 完全沒有任何處理——這三個只在「陳述式位置」(函式本體、let 自己的 body、match 某個分支自己的 body)才有真正的處理邏輯(`emit_body`/`emit_let`/`emit_match`/`emit_loop`,各自帶著真正的 `EmitScope` 正確串接新綁定)。修之前,這種形狀掉進 `emit_expr` 會直接落到通用的呼叫路徑,把 `let` 自己的 `[binding value]` 綁定向量整個當成一般的呼叫參數處理,對綁定的名字本身做 `scope_lookup`,當然找不到——這就是為什麼錯誤訊息永遠是「unknown identifier '<綁定名>'」而不是「unknown identifier 'let'」。這次刻意只做風險比較低的那一半:給一個真實、誠實、講得出問題所在的錯誤訊息,不是真正讓 `let`/`match` 可以直接當 `if` 條件式用(那個真正的修法——在 `if` 的條件式周圍合成一段 GNU C statement-expression,包住既有的陳述式 emitter——留著,列在 S223-01 裡沒關掉,是另外、更大的工作)。對任何原本就不會踩到這個形狀的程式完全沒有行為改變。新增 6 個回歸測試(`tests/test_emit.c`,`let`/`match` 各自的 parse-fine + 誠實報錯 + 錯誤訊息內容三項)。全套 342 個測試全過(既有 336 + 新增 6),0 failed。LO 自己的 emitter 測試也對著重新編譯出來的 `parena` binary 重跑過,全綠,S222-09 已經上線的 workaround 行為沒有變。commit `b616ed0`。(sess-20260830-1207-cc0ba7da)

## 2026-08-30
- 新增 stdlib/editor/registry.prn(S217-03):在 stdlib/editor/document.prn 上面做真正的多文件管理(open/switch/current/count)。跟 document.prn 自己的 apply-edit 一樣採「functional update」寫法——每個操作都回傳一個全新的 Registry,不是原地修改。switch-document 遇到超出範圍的 index 會保持原本的 registry 不變,已驗證過。同一套 `vec-T-at` `#target` 逃生口(這個 stdlib 已經在別的地方碰過好幾次的 elem-type-hint 缺口),這次是用在 struct(Document)元素上。關閉文件、改名、真正的持久化都列為真實、獨立的後續項目。`make test-editor-registry` 全綠、`-Werror` 乾淨。(sess-20260830-1207-cc0ba7da)
- 新增 stdlib/editor/document.prn:真正的文件管理(founder real-time: "we need document management asap")。把兩個已經做好、已經驗證過的東西接在一起,不是重新發明第三個:`editor/buffer.prn` 自己真正的文字/游標 Buffer,跟 `papercraft/note_version_mod.prn` 自己真正的合併/版本判斷邏輯(這次 session 稍早做的 S215-02)。真實、驗證過的行為:在 30 秒合併視窗內的編輯會直接覆蓋目前版本,超過視窗的編輯會真的開一個新版本(current-version 真的會+1)。誠實聲明範圍:目前只管一份文件(多文件管理列為真實、獨立的後續項目);版本歷史的實際內容沒有存在這裡——`note_version_mod.prn` 自己的範圍本來就只是決策邏輯,實際存哪裡(依照 `JEWEL/docs/NORTHSTAR_SARENA_NOTEBOOK.md` 的規劃是 IDUNA)是真正的、另外的主機端工作。`make test-editor-document` 全綠、`-Werror` 乾淨。(sess-20260830-1207-cc0ba7da)
- 新增 stdlib/http/router.prn:LO FRAMEWORK_NORTHSTAR.md 的 Phase B 驗證(founder real-time: "ok well write the deps in parena")。真正的、簡化版 Sinatra/Express 風格 router:單純的 `/` 分段加上一個 `:name` capture 慣例。刻意不重用 base4/pattern.prn 自己的 backtracking matcher——那個是對 base4 狀態向量操作,不是字元,而 FRAMEWORK_NORTHSTAR.md 自己已經把「怎麼把 byte 編碼成 base4」列為還沒決定的真實問題;這個檔案直接不等那個答案。過程中發現並修好一個真實的、原本潛藏著的 bug,是靠一次真正的 segfault 才抓到的:`vec-string-at` 的轉型(`*(char **)vec_get(v, idx)`,雙重解參考)是錯的——`String` 元素本身就已經是 `char *`,`string/split` 直接把它 push 進去,沒有像 `vec_box_i32`/`vec_box_f64` 那樣額外包一層(scalar 才需要另外配一塊記憶體),正確的讀法應該是單一次轉型。`regex/pcre.prn` 自己那份一模一樣的 `vec-string-at` 也有一模一樣的 bug——只是那邊唯一的呼叫者(`join-strings`)在整個 stdlib 裡完全沒有人真的呼叫過,所以從來沒被抓到過。兩邊都修了。`make test-http-router` 全綠、`-Werror` 乾淨。(sess-20260830-1207-cc0ba7da)
- 新增 stdlib/datetime.prn:Go 風格 magic reference-time layout 字串格式化(founder real-time: "add stdlibs for date time use the same magic string as golang")。`is-leap-year?`/`days-in-month`/`day-of-year`/`unix-parts`/`format-go-layout`。誠實聲明比 Go 自己的 `time` package 窄:只支援 `2006`/`01`/`02`/`15`/`04`/`05`(足夠應付 ISO-8601 這類時間戳),`06`/`03`/單位數形式/`PM`/`pm`、月份與星期名稱、時區偏移、小數秒都還沒做。發現並繞過一個真實的 compiler 缺陷:`src/emit.c` 裡有好幾個固定 `char buf[1024]` 的 buffer 用在 emit `#target`/`inline-c` 的內容——第一版把整個 format-go-layout(約 1.3KB)塞進單一 inline-c 字串,結果在約第 1033 個字元靜默截斷、完全沒報錯。這次是從架構上解決,不是單純補丁:`unix-parts` 改成用短的 `#target`(用 `gmtime` 搭配 compound-literal 指標,因為 `alloc` 目前只支援 `String`,而且這個 project 自己的 `-pedantic` build 會拒絕 GNU 的 statement-expression `({...})`),真正的 layout 替換掃描邏輯改用純 PARENA 的控制流寫,不再塞進一大塊 C。真正、直接驗證過的雙目標結果:`is-leap-year?`/`days-in-month` 現在真的可以編到 burrow 了——但是要先修好兩個在 BURROW 自己身上真實發現的缺陷(不是既有已知問題):`emit_go.go` 完全沒處理過裸的 `true`/`false` 字面值;defn 名稱結尾的 `?`/`!` 會產生不合法的 Go 識別字(修法是照抄 `src/emit.c` 自己既有的 `?`/`!` -> `_` 轉換慣例)。兩個都補了真正的回歸測試,而且是真的把產生出來的 Go 建置、跑過、確認輸出正確,不是只跑 unit test。`day-of-year` 因為用了 `loop`,還是碰不到 burrow(真實的、已經知道的既有邊界,這次沒動)。`format-go-layout` 本來就只能走 C——burrow 目前完全沒有字串組裝能力,跟 `k8s.prn` 已經踩過的邊界一樣。`make test-datetime` 全綠、`-Werror` 乾淨。(sess-20260830-1207-cc0ba7da)
- 新增 stdlib/papercraft/note_version_mod.prn:iCloud 風格的文件版本管理決策邏輯(founder real-time: "add parena primitives for managing versions of notes have it plug into papercraft - like the backend of icloud")。填補 notes_mod.prn 自己在 header comment 就已經誠實列出的缺口(真正的筆記文字/版本內容儲存還沒做)——這個檔案不儲存任何文字或版本內容,只負責真正需要的決策:何時把快速連續的編輯「合併」進目前版本而不是開新版本(模仿 iCloud 自己觀察到的真實行為——打字不會每個字元都開一個新版本,30 秒內視為同一次編輯),版本數超過上限(50,跟 note-slot-count 自己「數量要有界限」的慣例一致)時要淘汰哪一個舊版本(最舊的優先),以及真正的衝突判斷(用跟 iCloud/Google Docs/git fast-forward 一樣的 optimistic-concurrency 檢查:base 版本不是目前最新版才算真衝突,不是單純編輯時間接近就算)。8 個真實、手算過的斷言,`make test-papercraft-note-version` 全綠、`-Werror` 乾淨。(sess-20260830-1207-cc0ba7da)
- 新增 stdlib/mag/gematria.prn:把「the mag book」(QUEENSALLYONLINEBOOKOFMAGIFICATIONANDUNICOR)自己的 squish/gematria 演算法真正移植進 PARENA(founder real-time: "can we turn the mag book into parena playground?")。從已經驗證過的 Go 版本(`gpt2-alpine-c/pkg/towerprint`,本身已對過原始 Python/notebook 輸出——見 SECTION 147/S147-01)重新推導,不是憑空重讀 Python。`squish`/`az-digit`/`za-digit`/`fingerprint-az`/`fingerprint-za`。誠實聲明比 Go 版窄:fingerprint 用單純 I32 累加,不是 Go 的 `math/big.Int`——長字會溢位,短到中等長度的字驗證正確。`squish` 刻意用 `#target` 直接寫 C(固定大小輸出 buffer、單一寫入索引),不透過 Vec,因為 squish 只會讓字串變短。發現並繞過一個新的、同一類的真實 compiler 缺陷(跟 base4/*.prn 已經記錄的同一類):單純 `(let [n (string/length s)] ...)` 會把 `n` 綁成 `void *` 而不是 `I32`——改成每次用到就直接內嵌呼叫 `(string/length s)` 繞過。`self-test` 對照 `towerprint_test.go` 自己已驗證過的真實「SALLY」測資(AZ dec 20330、ZA dec 71661)——精確吻合。`make test-mag-gematria` 全綠、`-Werror` 乾淨。塔狀排版(視覺化的「魔法塔」)還沒移植,列為後續項目。(sess-20260830-1207-cc0ba7da)
- stdlib/base4/pattern.prn 新增 ALT/GROUP 支援(S208-09,LO 的 backlog 續作)。`elem-group`:真正、zero-width 的 GROUP(🗜)邊界標記(不消耗任何 target 狀態,在 `match-one`/`match-elem` 的量詞判斷之前就先處理,因為 group 需要跟一般 `One` 元素不同的位置運算)。誠實聲明範圍:目前完全沒有 capture 數值擷取——因為還沒有任何下游需要真正的擷取內容,這裡只保證 group 邊界不會破壞周圍的比對邏輯。`is-match-alt`:真正但刻意比 GRAMMAR.md 自己描述的「單一 flat Vec 內嵌 ALT token」窄——這是對兩個「分開的」pattern Vec 做二元選擇(先試 elems-a,失敗再試 elems-b),語意跟 `regex/pcre.prn` 自己的 `match-alt`(leftmost-first)完全一致。N 元(3 個以上)選擇跟真正把 ALT 內嵌進單一 flat Vec 的作法都列為真實的、之後才做的後續項目,這次沒做。`self-test` 新增 GROUP/ALT 的真實測試案例(拆成獨立的 `self-test-group-alt`,避免讓 `self-test` 自己的巢狀 `let` 疊得更深),`make test-base4-pattern` 全綠、`-Werror` 乾淨。(sess-20260830-1207-cc0ba7da)
- New stdlib/base4/pattern.prn (S208-07): LO/GRAMMAR.md §5.4's real PCRE-lite pattern matcher over base4 vectors, backtracking architecture directly reused from regex/pcre.prn's own proven design. Covers LoLanguageSpec.pdf's one fully-worked pattern example (anchors, literal, wildcard, all four quantifiers); ALT/GROUP deliberately deferred (S208-09). No Atom defenum (I32-payload match-destructuring confirmed broken, same class as vector/matrix's own documented gap) -- plain fields instead; match-seq returns a raw -1-sentinel I32, not Option I32, same reason. Second, separate, new gap found: building (Vec I32) test data from WITHIN .prn source (not external C) boxes literals as double, not i32 -- fixed with a new push-i32! (same #target-signature-resolves-types fix shape as vec-i32-at). A real, live OneOrMore off-by-one bug found and fixed via self-test's own hand-traced expectations catching it, not asserted correct. Real "write the main in pure PARENA" milestone (founder real-time): self-test is the real test orchestration, written entirely in .prn source since parena-c has no real main-emission convention yet (confirmed against src/emit.c) -- tests/test_base4_pattern.c is the thinnest possible external shim. make test-base4-pattern green, -Werror clean. STDLIB.md's own new "base4/pattern" section documents all of the above. (sess-20260830-1207-cc0ba7da)
- New stdlib/base4/matrix.prn (S208-06): LO/GRAMMAR.md §5.2's real STACK/MATMUL stdlib target. Base4Matrix (flat, row-major (Vec I32) + rows/cols, matching array.prn's own proven NDArray convention, not an unproven Vec-of-Vec). rows/cols/dims-eq/stack2 (2-row only, matching every real matrix example in LoLanguageSpec.pdf)/matrix-eq/matmul. Two real, genuine compiler gaps found and flagged, neither silently patched: (1) sibling same-named loop bindings in one function collide at C emission (worked around with distinct names); (2) matmul's per-cell accumulator hits the exact same double-boxing class already confirmed on base4/vector.prn's own dot -- verified directly (hand-traced [[2,3],[0,1]] product reads back wrong via the correct int* cast), not fixed here, same cross-cutting issue linalg.prn/base4/vector.prn already scoped out. make test-base4-matrix green, -Werror clean, everything but matmul's numeric output verified correct. STDLIB.md's own new "base4/matrix" section documents all of this. (sess-20260830-1207-cc0ba7da)
- New stdlib/base4/vector.prn: LO/GRAMMAR.md's real Phase 1 stdlib target (founder real-time: "continue working on lo adding to the stdlib libs necessary to make the language actually function... ffi into parena is acceptable if it satisfies the design"). vec-xor/vec-and/vec-or/vec-add/vec-subtract (elementwise base4 ops, dimension-gated, None on length mismatch), vec-eq (structural equality), dimlen (alias over vec/len), dot (AND-then-ADD accumulation -- a real design decision this file makes, since LoLanguageSpec.pdf never defines real multiplication over the base4 state space). Deliberately narrow: matrix stack/matmul and the PCRE-lite pattern matcher (GRAMMAR.md §5.2/§5.4) are real, separately-sized follow-ups, not attempted here. Real, genuine compiler gap CONFIRMED (same class linalg.prn's own header already documents, not a new bug): dot's loop accumulator seeded from an integer literal gets boxed as a double, silently violating its own declared (Option I32) return type -- verified directly in tests/test_base4_vector.c (a hand-traced dot product of 2 reads back as 0 via the correct int* cast). Not fixed here -- same cross-cutting loop-variable type-inference change linalg.prn already scoped as out-of-band. The other seven functions don't hit this and are real, verified end-to-end: 24 assertions, make test-base4-vector, -Werror clean. STDLIB.md's own new "base4/vector" section documents all of the above. (sess-20260830-1207-cc0ba7da)
- New stdlib/k8s/operator.prn: real Kubernetes operator-pattern (reconciliation-loop) primitives. Founder real-time: "build in the k8s operator pattern primitives". decide-action: real, standard four-way reconciliation decision (no-op/create/update/delete, modeled on controller-runtime's own real Reconcile contract). backoff-seconds/pow2: real exponential backoff with a cap, matching controller-runtime's own default rate limiter shape. result-for-action: real ReconcileResult (requeue, requeue-after-seconds) per action, real standard requeue policy (steady-state resync for no-op, quick verify for create/update, none for delete). Verified end-to-end via a real C test harness, all assertions pass. Real, honest, partial dual-target result: decide-action alone (no let, no construction) compiles through burrow's own Go target too, real go-test-verified; backoff-seconds/pow2 (use let) and result-for-action (constructs via {:field val}) don't -- two already-known, separate burrow v0 boundaries, not new breakage. Real, genuine third gap found writing this file and fixed in BURROW the same day, benefiting every future .prn file: unary (not x) was missing from both of burrow's own emitters entirely (fell through to a bogus call), the same real gap src/emit.c's own header already documented fixing on 2026-08-21 -- burrow just hadn't hit a real file using it yet. (sess-20260830-0908-5ede695f)

- New stdlib/k8s + stdlib/helm packages: real Kubernetes + Helm primitives. Founder real-time: "write kubernetes primitives into the stdlib and write helm support into parena both in the stdlib and also into DUNG and project BURROW" -> "helm primatives". stdlib/k8s/k8s.prn: real defstructs (Container, Deployment, ServiceSpec, PVCSpec, nested struct fields confirmed working) + real YAML-emitting functions, matching the exact real resource shapes PRRJECT_FATBABY/k8s/dashboard.yaml already needed. stdlib/helm/helm.prn: a real Chart defstruct + chart-yaml/values-yaml (matching helm create's own real scaffold conventions -- replicaCount, image.repository/image.tag) + a real templated-deployment-yaml substituting {{ .Values.X }} for image/replica fields. Both verified end-to-end: real parena build -> gcc -> a real C test harness (matching PAPERCRAFT/level_mod_test.c's own pattern) asserting the produced YAML against PRRJECT_FATBABY's own real dashboard values, all pass; real, working PRRJECT_FATBABY/charts/dashboard/ Helm chart generated from it, YAML-syntax-validated. Real, honest finding: cross-module calls only resolve correctly when all needed .prn files are passed together in one `parena build` invocation -- a standalone build of one file emits externally-prefixed symbol names that don't match a separately-compiled dependency's own unprefixed names. stdlib/k8s/scaling.prn: a separate, real, scalar-only companion (ScalingSpec + clamp-replicas, real HPA-style replica clamping) -- k8s.prn/helm.prn's own String+Arena-threaded string-building is fundamentally incompatible with burrow's Go emission target (GC-off-safe by design, no allocation reachable), confirmed directly (burrow build fails on the first Arena param); this file is scalar-only by design so it compiles through BOTH the C target and burrow's new Go target with real defstruct support (see BURROW's own CHANGELOG same day). (sess-20260830-0908-5ede695f)

- New stdlib/papercraft/notes_mod.prn: real first slice of EmilyOS-in-PAPERCRAFT (note-slot-count, slot validity, per-account access control). Cross-verified via two independent toolchains (parena build, burrow build). (sess-20260825-1938-f6bd411e)
- New stdlib/base4 package: real port of examples/engine.py.txt's own symbolic algebra (XOR/AND/OR/mod-4 add-subtract over a 4-symbol state space, plus cycle-length detection via VS0's typed Fn-callback params). Real bit-xor/bit-and/bit-or/mod primitive reuse, real C-vs-Python modulo correctness fix documented+tested. tests/test_base4.c: 24 assertions, make test-base4 green. (sess-20260825-1938-f6bd411e)
- emit_java: real Maven/Gradle package-declaration support (src/main/java/ convention auto-detected), needed for GTA7's real live Java consumer. New stdlib/gta7 package: humanness_fingerprint_mod.prn, real Enforcement mob reaction-delay tuning reusing MISHRI's own chance/random-int/gaussian-noise primitives via multi-file build. (sess-20260825-1938-f6bd411e)
- New stdlib/tyler/ package: cutscene_mod.prn, real decision logic (effect dispatch, trust/urgency trigger conditions) for TYLER's own FFXI-style cutscene spec, superseding the pre-PARENA GoblinFoxDragon/EduScript deferral. Also: usage text now mentions the .java build target (real gap from the earlier Java emitter commit). (sess-20260825-1938-f6bd411e)
- New v0 Java emitter (src/emit_java.c/.h): third PARENA compilation target after C/TypeScript. Same narrow v0 scope as emit_ts.c, Java-specific = -> ==, class-wrapper per output basename. tests/test_emit_java.c (23 assertions). Verified with real javac (JDK 25) against unmodified bezier_interp.prn/humanness.prn -- one PARENA source, three real compiled targets. (sess-20260825-1938-f6bd411e)
- Grew emit_ts.c's math primitive table (floor/sqrt/log/cos/pi); new stdlib/mishri/humanness.prn (chance/randInt/addNoise, real ports, verified bit-for-bit identical); math/random.prn renamed math.prn (sess-20260825-1938-f6bd411e)
- New v0 TypeScript emitter (src/emit_ts.c) -- scalar defn + single-expression body, wired into 'parena build ... -o *.ts'; new math/mishri stdlib packages; MISHRI's own bezierInterp proven as the first real, verified, bit-for-bit-identical proving-ground module (sess-20260825-1938-f6bd411e)
- New stdlib/papercraft/item_drop_mod.prn, inventory_mod.prn, pickup_mod.prn -- GTA3 item-drop + FFXI inventory decision logic (sess-20260825-1938-f6bd411e)

- New stdlib/papercraft/phone_mod.prn -- TYLER phone mechanics Phase 1 decision logic (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, struct-literal Option field + region-annotation field bug)
- stdlib/papercraft/talent_mod.prn: real talent-point allocation gate, ported from SHANKPIT_CONSTRUCT.txt's own progression_try_allocate. (sess-20260825-1938-f6bd411e)
- stdlib/papercraft/paper_fragment_mod.prn: real material-aware damage + threshold-based damage-tier decision logic for PAPERCRAFT's own Paper Engine destructible-geometry system. (sess-20260825-1938-f6bd411e)
- stdlib/papercraft/level_mod.prn: real RPG level-for-XP decision logic (PAPERCRAFT's first PARENA mod), corrected to use the real construct XP curve. (sess-20260825-1938-f6bd411e)
- selfhost/emit.prn: real "struct-literal field holding a freshly-constructed Result/Option value" support (a genuinely free widening -- struct-literal-field-value-supported? now accepts result-option-ctor-shaped?/none-shaped?, and emit-call-arg already dispatched both correctly), PLUS a real, pre-existing bug fix found while verifying it: a defstruct field with an explicit `@ Region` suffix (5 real children, not 3, the exact real shape String fields typically use) was silently rejected by struct-field-shaped?'s own old exact-3-children check -- never exercised by any prior struct-field test in this file. Widened to at-least-3 (type is always at index 2 regardless of a region suffix). Also widened struct-field-c-type to recognize Result/Option as real, by-value struct field types. 7 new tests including a real compile+run+assert check proving a struct literal constructs a struct whose String field AND whose freshly-constructed Option field both hold the correct values. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; cond/match as a call argument (by design); a struct-typed field used as a let-value or nested inside with-arena/cond/match; Vec/enum-typed defstruct fields; a struct literal in any position other than a defn's entire body; an I32-typed bare-symbol payload referencing a let-bound local. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, I32-typed bare-symbol Ok/Err/Some payload)
- selfhost/emit.prn: real I32-typed BARE-SYMBOL Ok/Err/Some payload support, closing the gap result-option-payload-is-i32?'s own header comment named explicitly as not-yet-attempted. Real, confirmed-live problem found via a direct probe: a bare symbol payload already satisfied the existing "pointer-shaped" branch unconditionally, silently emitting option_some(n) with n a raw C int passed where the runtime's own real void *value is expected -- a genuine -Werror=int-conversion compile failure (honest, not silent, but a real gap). Fixed with the same "special-case only where the info is already at hand" strategy as struct-return-type/struct-literal support: checks only whether the bare-symbol payload names one of the enclosing defn's own real, I32-typed params -- emit-defn-body special-cases this before the general path. New emit-defn-i32-param-ctor boxes the real, mangled param reference (via resolve-arena-ref, not the bare :text fallback only correct for a number literal) into the same real int_box helper. 6 new tests including a real compile+run+assert check proving the round trip on multiple inputs including a negative one, plus a direct regression guard proving a String-typed bare-symbol payload (the dominant real case) is genuinely unchanged. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; cond/match as a call argument (by design); a struct-typed field used as a let-value or nested inside with-arena/cond/match; Vec/enum-typed defstruct fields; a struct literal in any position other than a defn's entire body; a struct-literal field holding a freshly-constructed Result/Option value; an I32-typed bare-symbol payload referencing a let-bound local rather than a param. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, struct-literal construction, closing the defstruct gap pair)
- selfhost/emit.prn: real struct-LITERAL construction support, closing "general struct-literal construction for a user-defined defstruct" -- the other of the two real gaps the defstruct section's own header comment named since defstruct support first landed. Scoped narrow: a defn whose ENTIRE body is one map-literal matching an already-registered struct (named by the defn's own declared return type) exactly -- emit-defn special-cases this the same way it special-cases struct-returning-get-field-body?, avoiding the invasive whole-file threading a general "struct literal anywhere" feature would need. Emits a real C99 compound literal ((Point){x, y}, positional, matching declared field order) rather than the reference compiler's own Point_new(x, y) constructor-function convention -- simpler, no new per-struct function needed. struct-prepass widened to build a second, parallel known-struct-nodes registry alongside known-structs. Found and fixed a real, live type-mismatch while building this: Option-Node/deref-based lookups repeatedly failed real gcc compiles (the reference compiler's own deref needs a concrete declared variable type to hook into, which a bare Option match value position doesn't reliably provide) -- redesigned around plain I32 indexes into the parallel registries instead, sidestepping Option-of-struct boxing entirely. A second real gap found the same way: a mismatched struct literal used to fall through to a generic `return ;` (still an honest compile failure, but not this file's own established real #error-directive convention) -- fixed with a dedicated map-literal-shaped? clause in emit-form. 7 new tests including a real compile+run+assert check proving construction works with the map literal's own keys deliberately reordered from the struct's declared field order, plus a negative test for the mismatch case. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; an I32-typed bare-symbol Ok/Err/Some payload; cond/match as a call argument (by design); a struct-typed field used as a let-value or nested inside with-arena/cond/match; Vec/enum-typed defstruct fields; a struct literal in any position other than a defn's own entire body. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, real I32 Ok/Err/Some payload construction, closing the full round trip)
- selfhost/emit.prn: real, non-pointer-shaped (I32) Ok/Err/Some payload CONSTRUCTION support, closing the gap named repeatedly this session -- the natural complement to the same day's earlier real deref support, together completing the FULL round trip: a value can now be both produced and consumed, scalar payload included, entirely by real selfhost-emitted C. New int-box-helper-decl emits a genuine, minimal, narrow port of the one real case src/emit.c's own much larger, general ensure_box_helper mechanism exists for -- a single, hardcoded int_box(Arena *dest, int v) helper, unconditionally emitted every program (deliberate simplification over the reference's own on-demand, deduplicated generation, since this narrow v0 only ever needs one type). New result-option-payload-is-i32? recognizes a bare number literal or a binary-op-call-shaped? expression; new arena-scope-any-ref finds the destination arena to box into (most-recently-pushed binding in scope, honest self-documenting-invalid-identifier fallback for the genuinely degenerate no-arena case). An I32-typed bare-symbol payload is deliberately not included (no per-symbol type tracking in this narrow emitter) -- a real, separate, not-yet-attempted gap. 7 new tests including a real compile+run+assert check (tests/integration/driver_i32_roundtrip.c) proving a real I32 value genuinely round-trips through a real heap box, a real Option construction, and a real match+deref consumption, construction AND consumption both entirely selfhost-emitted. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; an I32-typed bare-symbol Ok/Err/Some payload; cond/match as a call argument (by design); a struct-typed field used as a let-value or nested inside with-arena/cond/match; Vec/enum-typed defstruct fields; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, real deref support + a 6th real bug fix)
- selfhost/emit.prn: real `deref` support, closing the "a clause body that needs the payload beyond a bare-symbol tail" half of the match gap named since match support first landed. `(deref x)` reads back a real, boxed I32 value out of a match-bound void* payload local, emitting `*((int *)(x))`, boxed via emit-i32-boxed when crossing this file's own char* boundary. Constructing a real boxed I32 payload is the natural complement but a real, separate, harder gap not attempted (needs new box-helper-generation infrastructure) -- verified here against a value boxed directly in C by the test driver, the same technique driver_match.c's own original version already used. Found and fixed a real, live, silently-wrong-C bug while verifying (the same bug class caught 5 times already this same day): `deref` was also never excluded from plain-call-shaped?'s own name checks -- `(deref x)` used to silently emit a call to a never-defined `deref` function. Fixed by adding `deref` to other-special-form-symbol?'s own exclusion list. 6 new tests including a real compile+run+assert check (tests/integration/driver_deref.c) proving the real dereference genuinely reads back the correct value from a real boxed I32 payload on multiple inputs including a negative one. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a real, non-pointer-shaped Ok/Err/Some payload constructed by this emitter itself; cond/match as a call argument (by design); a struct-typed field used as a let-value or nested inside with-arena/cond/match; Vec/enum-typed defstruct fields; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, get-field as a let-value)
- selfhost/emit.prn: real get-field-as-a-let-value support, closing a real, honest, until-now-unsupported gap confirmed live via a direct probe: (let [n (get-field p :x)] n) fell straight to the honest #error fallback, get-field-shaped? never having been wired into emit-let-value at all (not even for a plain scalar field). Scalar-only v0, boxed via emit-i32-boxed -- the same assumption emit-form's own tail-position dispatch already makes. A struct-typed field as a let-value would be wrongly boxed the same way a struct-typed field used to be wrongly boxed in tail position -- but unlike a defn's own body, a let-binding carries no declared return-type annotation to special-case against, so there's no equivalent narrow fix here without real field-type lookup infrastructure; not attempted. 6 new tests including a real compile+run+assert check (tests/integration/driver_let_get_field.c) proving the real scalar struct-field read genuinely computes the correct value through a real let-binding on multiple inputs including a negative one. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; cond/match as a call argument (by design); a struct-typed field used as a let-value or nested inside a with-arena/cond/match body; Vec/enum-typed defstruct fields; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, struct-typed field as a tail-position return)
- selfhost/emit.prn: real struct-typed-field-as-a-tail-position-return support, closing the one gap struct-return-type support's own landing explicitly left open the same day. emit-form's own get-field-shaped? dispatch always boxes via emit-i32-boxed, correct only for a scalar field -- boxing a real struct value that way isn't even valid C. Rather than threading struct-type-awareness through emit-form's own general recursion (used from many contexts, a bigger undertaking), new struct-returning-get-field-body? checks only the one case where the needed information is already at hand: a defn whose own declared return type is an already-registered struct AND whose entire body is exactly one get-field-shaped? form -- emit-defn then bypasses the general emit-body-forms/emit-form path, emitting a real, unboxed return directly via emit-get-field. Every other case (nested inside let/with-arena/cond/match, a scalar-field tail, or a non-get-field body) keeps pre-existing behavior unchanged. 7 new tests including a real compile+run+assert check (tests/integration/driver_struct_field_tail.c) proving the returned struct carries both real fields correctly on multiple inputs including negative values, plus a direct regression guard proving the pre-existing scalar-field case still emits the exact same boxed form as before. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; cond/match as a call argument (by design); a struct-typed field nested inside a let/with-arena/cond/match body; Vec/enum-typed defstruct fields; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, struct return types + non-get-field call as a get-field target)
- selfhost/emit.prn: real struct-return-type support PLUS a non-get-field nested call as a get-field target, closing two still-open gaps at once -- naturally paired, since a function returning a struct BY VALUE is the real, motivating reason a get-field target ever needs to accept a plain call in the first place. defn-c-return-type widened (known-structs/dest threaded to its 2 real call sites) to recognize a declared return type naming an already-registered struct, the same by-value convention param-c-type/struct-field-c-type already established -- needed since a bare struct-typed param passed straight through produces a real struct C value via emit-tail-symbol's own unboxed return, correct only when the function's own declared C return type matches. get-field-shaped?/emit-get-field widened a third time the same day to accept a plain-call-shaped? target too, emitting via the same emit-plain-call this file already trusts, composing (identity_point(p)).x -- real, terminating mutual recursion with plain-call-shaped?/every-call-arg-symbol-or-number? (safe since every recursive call strictly descends into a smaller child subtree of a finite Node tree). 7 new tests including a real compile+run+assert check (tests/integration/driver_struct_return_type.c) proving a real struct value genuinely round-trips through a real by-value function return and a real get-field read on multiple inputs including a negative value. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; cond/match as a call argument (by design); a struct-typed field as a tail-position return; Vec/enum-typed defstruct fields; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, nested/chained get-field)
- selfhost/emit.prn: real nested/chained get-field support (line.start.x-style), closing the "get-field on a NESTED expression" gap named the moment get-field support first landed -- real friction hit firsthand while verifying struct-typed struct field support the same day. get-field-shaped?'s own target check widened to accept a bare symbol OR itself a nested get-field-shaped? node; emit-get-field's own target resolution widened to recurse when the target is itself get-field-shaped, composing ((l).start).x. Real, honest, narrower-than-general scope: a chain of get-field calls all the way down to a bare-symbol root only -- a non-get-field nested call as a get-field target is still a real, separate, harder case, not attempted. 3 new tests including a real compile+run+assert check (tests/integration/driver_nested_get_field.c) proving the real chained field read genuinely computes the correct value on multiple inputs. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; cond/match as a call argument (by design); a struct-typed field as a tail-position return; a non-get-field nested call as a get-field target; Vec/enum-typed defstruct fields; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, struct-typed struct fields)
- selfhost/emit.prn: real struct-typed struct field support, closing one of the two real gaps the defstruct section's own header comment named the moment defstruct support first landed. Threaded known-structs (the same registry param-c-type already established) through struct-field-c-type/struct-field-supported?/struct-field-shaped?/all-struct-fields-shaped?/defstruct-shaped?/emit-struct-field/emit-struct-fields/emit-defstruct -- a struct-typed field now recognized and emitted with the real struct's own name as its C type, by value. A struct-typed field's own struct must be registered already (declared earlier in the file) -- not an arbitrary scope choice, the same real requirement plain C itself imposes on by-value struct nesting (an incomplete type can't be embedded by value). Found and fixed a real, small formatting bug while verifying: the new struct-lookup branch, copied from param-c-type's own convention (trailing space), produced a genuine double space ("Point  start;") once composed with emit-struct-field's own separate explicit space push -- fixed by matching struct-field-c-type's own sibling branches' convention (no trailing space) instead. 7 new tests including a real compile+run+assert check (tests/integration/driver_struct_field_type.c) proving a genuinely nested Point-inside-Line struct read produces the correct value at runtime, exercised via the call-argument position (a struct-typed field used as a tail-position body would hit a real, separate, already-documented, pre-existing gap -- get-field's own tail-position dispatch always boxes via emit-i32-boxed, scalar-only -- not attempted here, not a regression). Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; cond/match as a call argument (by design); a struct-typed field as a tail-position return; Vec/enum-typed defstruct fields; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, forward prototypes)
- selfhost/emit.prn: real forward-prototype support, closing the "no forward prototypes emitted" gap named repeatedly the same day -- several earlier test fixtures had to reorder functions (callee before caller) specifically to work around it. New emit-defn-prototype reuses the exact same signature-building pieces emit-defn's own body already does (mangle/emit-params/defn-c-return-type) -- a prototype is just that signature text plus `;` instead of a body. emit-program now runs a real prototype pre-pass over every top-level defn before any bodies, matching the C reference emitter's own real, established convention (build_defn_prototype) of emitting one unconditionally for every defn. 5 new tests including a real compile+run+assert check (tests/integration/driver_forward_ref.c) proving a real 2-function program in caller-before-callee order -- the exact ordering that used to trigger a real gcc "implicit declaration" error -- now compiles clean and computes the correct value. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean, including the multi-file build-files domain's own real negative case (b.prn alone still genuinely fails without a.prn). Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; cond/match as a call argument (by design, not attempted); defstruct fields typed as another struct/Vec/enum; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, or/and/not as call arguments + 3 more real bug fixes)
- selfhost/emit.prn: real or/and/not-as-a-call-argument support (`(f (and a b))`), closing the last real exclusion from the prior round's own still-open list. emit-bool-expr already emits a plain, raw C boolean/int expression needing no boxing. While building this, found and fixed THREE more real, live, silently-wrong-C bugs of the exact same class (confirmed via direct probes, not guessed): `(f (Some s))` was silently emitting `f(Some(s))` (a call to a never-defined function `Some`, since Ok/Err/Some were never excluded from plain-call-shaped?'s own name checks -- fixed with a new result-option-ctor-symbol? exclusion, PLUS real support: Ok/Err/Some construction as an argument now emits the real result_ok/result_err/option_some runtime call); a bare `(f None)` was silently emitting `f(None)` (referencing a never-declared identifier -- fixed with a new emit-call-arg dispatch clause emitting real option_none()); `(f (cond (b 1) (true 2)))` was silently emitting THREE bogus calls at once (`f(cond(b(1), true(2)))`, since a cond clause's own 2-element list shape also satisfies plain-call-shaped?'s recursive argument check -- fixed with a new other-special-form-symbol? exclusion covering cond/match/with-arena/let, real defense in depth rather than relying on with-arena/let's own accidental structural safety). 10 new tests: crash-regression fixture moved a 4th time (bool-expr -> cond-as-argument), plus 3 new positive-coverage blocks including 2 real compile+run+assert checks (driver_bool_arg.c, driver_ctor_as_arg.c). Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; cond/match as a call argument (by design, not attempted); no forward prototypes emitted; defstruct fields typed as another struct/Vec/enum; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, string literals as call arguments + a real bug fix)
- selfhost/emit.prn: real string-literal-as-a-call-argument support (`(f "lit")`), closing the "no literals" half of a gap named since the very first binary-op/plain-call work (numbers closed same-day back then; strings stayed open until now). New emit-string-literal re-wraps the literal's own real, already-decoded :text in C double-quotes -- same honest, no-re-escaping convention emit-alloc-call's own literal argument already relies on. Found and fixed a real, live, silently-wrong-C bug while verifying: `(f (and b b))` as a call argument was silently emitting `f(and(b, b))`, a call to a never-defined C function named `and`, instead of the correct boolean logic or an honest #error -- plain-call-shaped? never excluded or/and/not from its own name checks the way alloc and every binary-op-symbol? name already are, harmless until the earlier same-day nested-call-argument widening made plain-call-shaped? itself decide whether a NESTED argument is supported (a position or-and-shaped?/not-shaped? were never wired into). Fixed with a new bool-op-symbol? exclusion; `(and b b)` as an argument now correctly falls through to a clean #error. 6 new tests: the crash-regression fixture moved a third time (string literal -> `or`/`and` boolean expression, proving the fix), plus real positive coverage including a compile+run+assert check (tests/integration/driver_string_literal_arg.c). Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; or/and/not as a call argument; no forward prototypes emitted; defstruct fields typed as another struct/Vec/enum; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, nested alloc calls as call arguments)
- selfhost/emit.prn: real nested-`alloc`-call-as-a-call-argument support (`(f (alloc dest String "lit"))`), closing the one real exclusion the earlier same-day nested-call-argument work explicitly named as still open. Turned out not to be a real obstacle: alloc-call-shaped?/emit-alloc-call already take the same scope/dest every other expression emitter here does, and the target Arena is always named explicitly in alloc's own syntax -- no boxing ambiguity either, since emit-alloc-call's own real return (arena_strdup(...)) is always pointer-shaped. every-call-arg-symbol-or-number? gained a 4th accepted shape; emit-call-arg gained the matching dispatch. The crash-regression fixture that had already moved once this same day (nested call -> nested alloc call) moved a second time, to a genuinely different still-unsupported shape: a raw string literal as a call argument (no shape guard accepts kind NString at all yet). New positive coverage including a real compile+run+assert check (tests/integration/driver_nested_alloc.c) proving a real 2-function program with a nested alloc call as an argument emits no #error, nests inline, compiles clean, and genuinely allocates the correct string into the correct Arena at runtime. Also confirmed, in passing, a real, separate, pre-existing limitation the test fixture had to work around: emit-program emits no forward prototypes, so a callee must textually precede its own caller -- unrelated to this feature, not attempted here. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; a raw string literal as a call argument; no forward prototypes emitted; defstruct fields typed as another struct/Vec/enum; general struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, nested calls as call arguments)
- selfhost/emit.prn: real nested-call-as-a-call-argument support (`(f (g x))`), closing the "nested calls as call arguments" gap this file's own header comments named but didn't fix since the very first binary-op/plain-call work. every-call-arg-symbol-or-number? (the shared shape-guard binary-op-call-shaped?/plain-call-shaped? both delegate to) now also accepts a nested plain-call-shaped?/binary-op-call-shaped? argument, emitted through the SAME emit-plain-call/emit-binary-op this file already trusts for a whole function body -- makes the 3 functions real, terminating mutual recursion (a Node tree is always finite). emit-call-arg gained matching dispatch (get-field, plain-call, binary-op, bare-symbol/number fallback). Found and confirmed live (direct compile+run, not guessed): testing with two I32-typed functions surfaced that the generated C for an I32-returning nested call composed into a binary-op is genuine POINTER arithmetic (char* + int, since every function's real declared C return type is char*) -- still produces the numerically correct result on every input including negative operands, because char has size 1 (advancing a char* by N bytes is bit-for-bit identical to plain int addition once round-tripped through (int)(intptr_t)result) -- not a new risk, the same pre-existing emit-i32-boxed convention exercised in a new position. 4 new tests: the original plain-call crash-regression fixture's own negative case moved from "nested call as argument" (no longer unsupported) to "nested alloc call as argument" (still genuinely unsupported, preserving the crash-safety guard honestly); new positive coverage including a real compile+run+assert check (tests/integration/driver_nested_call.c) proving a real 2-function program with a nested call as one operand of a binary-op emits no #error, nests inline in the generated C, compiles clean, and computes the correct value on every real input. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; a nested alloc call as a call argument; defstruct fields typed as another struct/Vec/enum; general struct-literal construction for a user-defined defstruct. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, Result/Option construction, closing the match round trip)
- selfhost/emit.prn: real, narrow Result/Option CONSTRUCTION support (`Ok`/`Err`/`Some`/`None`), the direct complement to match support -- closes the "constructing an Ok/Err/Some/None value from PARENA itself" gap flagged the moment match landed, completing a real round trip (produce AND consume a Result/Option entirely within this emitter's own domain). Payload must already be pointer-shaped (bare symbol / get-field-shaped? / plain-call-shaped?) -- an I32 payload deliberately not supported (reusing emit-i32-boxed's own intptr_t-reinterpret trick here would hand the runtime a bogus, non-dereferenceable pointer; a real per-type heap box is separate, harder, unattempted work). Found and fixed a real, deeper gap while building this: emit-defn hard-coded every function's own C return type as `char *` unconditionally -- but `(Ok s)`'s own real body produces an actual `Result` C STRUCT, which can't be returned from a `char *`-declared function. New defn-c-return-type/defn-declared-return-type-name read a defn's own explicit `: ReturnType` annotation and recognize `Result`/`Option` by name as the real, concrete C return type; every other declared type (including none) keeps the pre-existing `char *` default unchanged. 11 new tests (tests/test_selfhost_emit.c), including a real compile+run+assert round-trip check (tests/integration/driver_result_ctor.c) constructing every real Result/Option value via real selfhost-emitted make-ok/make-err/make-some/make-none, feeding each into real selfhost-emitted round-trip-result/round-trip-option, asserting tag AND payload both survive intact. Zero regressions: 336-test suite + all 6 selfhost domain test binaries + test-json/test-yaml + bazel build/test //... all clean. Still open: general user-defenum tag registry; match/cond as a non-tail value; a match clause body using the payload beyond a bare-symbol tail; a non-pointer-shaped Ok/Err/Some payload; nested calls as call arguments generally; defstruct fields typed as another struct/Vec/enum; general struct-literal construction for a user-defined defstruct. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, construct-split Spotlight mod)
- selfhost/emit.prn: real, narrow, TAIL-POSITION-ONLY Result/Option match support, closing the first (dominant) half of the match gap named in every prior selfhost entry. Hardcodes the one real tag mapping (Ok/Some=1, Err/None=0) this whole stdlib's match usage already relies on rather than building a general user-defenum tag registry (real, separate, harder work, still open). Scrutinee: a bare symbol or a plain-call-shaped? call, evaluated once into a real, block-scoped 'Result __match_scrutinee'/'Option __match_scrutinee' local (a fixed name is safe since match always wraps itself in a fresh { } block). Exactly 2 clauses (bare None, or (Ok x)/(Err e)/(Some s)), payload bound to a real void* local. Also added Result/Option as real param-c-type cases (by value), needed to make a match's own scrutinee reachable via a function signature at all. 9 new tests (tests/test_selfhost_emit.c, 44 total for domain 4), including a real compile+run+assert check (tests/integration/driver_match.c) against real constructed Result/Option values across every tag. Zero regressions: 336-test suite + all selfhost domains clean; bazel build //... blocked by the same pre-existing, already-documented /home/treeiii/.cache permission collision from a separate concurrent session, not a regression. Still open: general user-defenum match, match as a non-tail value, a clause body using the payload beyond a bare-symbol tail (needs deref-form emission), constructing Ok/Err/Some/None from PARENA itself. (sess-20260825-1938-f6bd411e)
- Real .tmLanguage.json grammar loader (stdlib/editor/textmate_loader.prn) + first language mod, JSON (stdlib/editor/lang_json.prn), wired into the editor for real .json syntax highlighting. Fixed 3 real top-level name collisions (json/regex/array/expr). Structural CI fix: editor-demo's source list now lives in one shared file (examples/editor-demo-sources.txt), not two drifting copies. commits 45a0b51/d612408. (sess-20260825-1938-f6bd411e)
- New stdlib/editor/construct-split.prn, a real PARENA mod (not hand-written C) hooking into the Ctrl+T/Cmd+T Spotlight overlay -- founder real-time: "i want this as a parena mod - have it hook into the ctrl t quick open pane when i have that open if i type /construct-split 10 if it is a construct file it should use file start and file end to open up new panes with the chunks of the file broken into roughly equal 10 sizes its not gonna be totally equal". Typing `/construct-split N` while a real `*_CONSTRUCT.txt` bundle (the same `--- FILE START: ---`/`--- FILE END: ---` format ci.yml's own "Generate Construct Bundle" step produces) is the active buffer offers a real SKConstructSplit Spotlight result; activating it writes N real chunk files (a greedy bin-pack over file boundaries only -- splits never happen mid-file, only ever between consecutive FILE START markers, so "roughly equal, not totally equal" per the founder's own explicit framing) and opens each in a real new pane via the existing spawn_new_instance mechanism (this editor's only real multi-window primitive; there is no in-process split-view). Mirrors the existing SKFile architectural split exactly: the PARENA mod owns 100% of the real algorithmic work (command parsing, construct-file detection, the actual byte-accurate split), the C driver (examples/editor_main.c) owns only the unavoidable host I/O (temp file write, process spawn). New dedicated test (tests/test_construct_split.c, make test-construct-split, wired into ci.yml) verifies parse-construct-split-count/construct-file?/split-construct directly: 400-synthetic-file correctness (every file found in exactly one chunk, none duplicated/dropped), chunk-count bounds, rough size balance, and honest fallback (a non-construct buffer returns the whole text as one chunk rather than crashing). Also extended tests/test_editor_spotlight.c's own run-providers coverage for the new current-buffer-text parameter. Zero regressions: 336-test suite + test-editor-spotlight (13/13) + test-construct-split (13/13) + full editor-demo rebuild (clean, zero warnings) + Xvfb smoke run (clean timeout exit, no crash) + bazel build/test clean. Honest still-open scope: temp chunk files are deliberately never cleaned up (would race the spawned child's own read schedule); no xdotool in this sandbox so real keyboard-driven Ctrl+T-then-type-then-Enter interaction was verified at the function level (run_providers/split_construct called directly with real construct-formatted text) rather than through the actual SDL2 event loop. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, large-file open perf)
- runtime/parena_runtime.h: fixed a real, confirmed-live perf bug in raw_read_all_impl (read-string's own host primitive, the PARENA editor's own file-open path) -- founder real-time: "when i open a large file its dog shit slow we dont want to load the whole thing into memory". The fstat fast path (sizing the buffer to the real file size up front) was still silently paying for a full SECOND copy of the whole file: detecting real EOF needs one more nonzero-sized read() call, and the old exact st_size+1 sizing left no room for it, so the growth check fired once right at the end -- one full, needless doubling, confirmed directly with a real 119MB test file (cap ended up exactly 2x the real file size). Fixed by sizing the buffer with real headroom (one full extra 4096-byte read-chunk) so the confirming read never needs to grow first, and tracking whether a real overshoot doubling ever actually happened (not an exact size comparison) to decide whether the old trim-copy is still needed. Confirmed live: a real 119MB file's own editor-open RSS dropped from ~479MB to ~247MB (halved) and open time from 0.66s to 0.48s. New real regression test (tests/test_editor_io.c) walks the Arena's own real block chain after a ~4MB file read and asserts total capacity stays well under 1.5x the file size, a direct guard against this exact class of regression. Zero regressions: full local suite (336 tests) + test-editor-io + test-json + test-yaml + bazel build/test clean. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, top-level or/and/not)
- selfhost/emit.prn: real top-level or/and/not support (tail position AND let-value), closing the gap the defstruct work above flagged as found-but-not-fixed. bool-expr-supported?/emit-bool-expr (already built for cond's own test position) were only ever reached from that dispatch -- a defn whose ENTIRE body is `(and ...)`, the natural shape for a real predicate function like is-origin?, silently emitted an empty `return ;`. Two new emit-form clauses close the tail-position half (boxed via emit-i32-boxed); a new shared let-value-is-bool-expr? closes the symmetric let-binding-value half. 2 new tests (35 total for domain 4), including a real compile+run+assert check combining both positions in one program (is-origin?'s own top-level and body composing get-field inside a comparison, plus is-boring's own or used as a let-value). Zero regressions: 336-test suite + all 6 selfhost test binaries + bazel build clean. Still open: match itself, cond as non-tail value, nested call arguments generally, struct-typed struct fields, struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, defstruct + get-field + mangle fix)
- selfhost/emit.prn: real top-level defstruct support, closing the other large standalone self-hosting gap alongside cond/match. Narrow v0: scalar fields only (I32/Bool/F64/String/Arena), no struct-literal construction. New struct-prepass emits a real typedef + registers struct names before emit-program's own defn pass, matching the reference compiler's own "structs before defns" ordering. param-c-type (threaded a new known-structs registry) now passes a registered struct type BY VALUE (`Point p`), confirmed against the reference's own real generated shape first. New get-field-shaped?/emit-get-field (`(get-field p :x)` -> real `(p).x`), wired into emit-call-arg (so get-field composes inside comparisons/plain-calls) and emit-form's tail position (a real accessor's natural shape, boxed). Found and fixed a real, separate, pre-existing gap while verifying: mangle() only ever converted a hyphen, so `is-zero-x?` emitted as the literal invalid C identifier `is_zero_x?` -- widened to match the reference's own real mangle() (also converts `/`/`!`/`?`, strips a leading `!`). 5 new tests (33 total for domain 4), including a real compile+run+assert check against real constructed Point values. Zero regressions: 336-test suite + all 6 selfhost test binaries + bazel build clean. Found but NOT fixed: and/or/not at the top level/tail position (not inside a cond test) still isn't handled by emit-form at all, flagged honestly for later. Still open: match itself, cond as non-tail value, nested call arguments generally, struct-typed struct fields, struct-literal construction. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, predicate-call cond tests)
- selfhost/emit.prn: real plain-call-shaped predicate function calls as a `cond` test -- closes the second (and last) of the two real exclusions the same-day cond work explicitly named. Needed no new boxing/unboxing machinery: a Bool-returning function's own body already boxes 0/1 as a real NULL/non-NULL char* on the callee side (emit-i32-boxed, via emit-form's binary-op-call-shaped? branch), and a real C `if (char_star_expr)` already treats non-NULL as truthy -- zero extra casting needed at the call site. bool-expr-supported?/emit-bool-expr gained one more clause each (plain-call-shaped? -> emit-plain-call); the existing or/and/not recursion means a predicate call also composes for free inside compound tests. 2 new tests (27 total for domain 4), including a real compile+run+assert check against a genuine two-function program (a real predicate plus a real cond dispatching to it). Zero regressions: 336-test suite + all 6 selfhost test binaries + bazel build clean. Still open: match itself, cond as a non-tail value, nested call arguments generally, defstruct not walked. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, or/and/not compound cond tests)
- selfhost/emit.prn: real `or`/`and`/`not` compound test support for `cond` -- closes one of the two real exclusions the same-day cond work explicitly named. New, real, RECURSIVE bool-expr-supported?/emit-bool-expr sub-language (replacing the narrower cond-test-supported?/emit-cond-test): or/and (binary, this codebase's own real "nest pairs" convention) and not (unary), each recursively composing further comparisons or bool-expr sub-expressions -- `(or (= n 1) (= n 3))` emits real `((n == 1) || (n == 3))`. Deliberately doesn't generalize to arbitrary nested calls as arguments (the genuinely harder, still-open gap) -- a boolean/test context never crosses this emitter's own char*-boxing boundary, so it sidesteps that problem entirely. 2 new tests (25 total for domain 4), including a real compile+run+assert-across-7-branches check (tests/integration/driver_bool_expr.c). Zero regressions: 336-test suite + all 6 selfhost test binaries + bazel build clean. Still open: match itself, cond as a non-tail value, plain-call-shaped predicates as a cond test, nested call arguments, defstruct not walked. (sess-20260825-1938-f6bd411e)

## 2026-08-28 (continued, cond support)
- selfhost/emit.prn: real, tail-position-only `cond` support -- closes what was the single largest remaining self-hosting gap (match/cond had no dispatch in emit-form at all). A real if/else-if/else chain is tractable in tail position (every clause's own result is already a complete return statement, via emit-form itself, recursively) even though a non-tail match/cond genuinely isn't (would need something like GCC's own statement-expression extension, incompatible with -std=c99 -pedantic). New cond-call-shaped?/emit-cond/emit-cond-clauses/emit-cond-test, wired into emit-form only (never emit-let-value -- a cond as a let-value IS the hard non-tail case). Narrow v0: match itself not attempted; a test is a binary-op comparison, a bare symbol, or literal true; or/and/not compound tests and predicate calls not attempted; the last clause must be (true ...) or the whole cond is honestly not treated as supported. Found and fixed a 5th gap while verifying: a bare number literal as a tail-position result (any cond clause's own `10`) hit the same unboxed-int-into-char* problem emit-i32-boxed already exists for binary-op -- same fix, new call site. 4 new tests (23 total for domain 4), including a real compile+run+assert-on-all-three-branches check (tests/integration/driver_cond.c) and a real negative case (no trailing true clause -> honestly not treated as cond-shaped). Zero regressions: 336-test suite + all 6 selfhost test binaries + bazel build clean. Still open: match itself, cond as a non-tail value, or/and/not test support, nested call arguments, defstruct not walked at top level. (sess-20260825-1938-f6bd411e)
- selfhost/emit.prn: real binary-op support (`+`/`-`/`*`/`/`/`=`/`>=`/`<=`/`>`/`<`) as real C infix syntax, closing the "no literals" half of a previously-flagged gap -- `every-call-arg-symbol?` (renamed every-call-arg-symbol-or-number?) now accepts a number-literal argument, not just a bare symbol. Found and fixed 2 more real gaps while verifying: (1) an arithmetic result is a real unboxed `int` but this emitter declares every function/let-binding `char *` -- new emit-i32-boxed reinterprets via a real `(char *)(intptr_t)` cast (not heap allocation), verified as a genuine value round-trip (not just "compiles clean") via a new driver (tests/integration/driver_arith.c) that actually calls the compiled function and asserts the real returned value. (2) emit-body-forms always started its own body walk at a hard-coded index 3, so a defn with an explicit `: ReturnType` annotation (the exact `] : I32` shape kind-code/is-close-token?/etc already use, or the even more common `] : String @ Region`) had its own colon/type/at/region children wrongly walked as 2-4 extra bogus body statements -- new body-start-index detects and skips the annotation. 4 new tests (19 total for domain 4). Zero regressions: 336-test suite + all 6 selfhost test binaries + bazel build clean. Still open: nested calls as arguments, match/cond as an entire defn body, defstruct not walked at top level. (sess-20260825-1938-f6bd411e)
- selfhost/emit.prn: emit-form now also handles a bare `alloc` call or a bare plain-function-call as a defn's ENTIRE body (not just with-arena/let) -- one of the 3 real gaps found the same day while building build-files' own multi-file test fixture. Previously fell through to emit-tail-symbol, which only correctly handles a bare symbol, silently emitting an empty `return ;` for anything else. New shared emit-tail-expr wrapper reuses the existing emit-alloc-call/emit-plain-call expression emitters (already proven correct for let-binding values) rather than duplicating them. 2 new tests (test_selfhost_emit.c, 15 total for domain 4), verified via both direct emitted-C assertions and a real, live build_files round-trip. Bare arithmetic and bare match/cond as an entire body remain real, separate, still-open gaps. Zero regressions: 336-test suite + all 6 selfhost test binaries + bazel build clean. (sess-20260825-1938-f6bd411e)
- selfhost/main.prn: build-files, the real multi-file entry point (cmd_build's own multi-file merge, ported) -- parses each input path, merges every file's top-level children into one combined unit, region-analyzes/emits/writes exactly once. Verified via direct C-level instrumentation (merge itself proven correct: 2 real files' children genuinely combined) and a new dedicated end-to-end fixture + test (examples/selfhost_multifile_a.prn/_b.prn, tests/test_selfhost_main_multifile.c, make test-selfhost-main-multifile): both files built together produce real gcc-clean C with a genuinely resolved cross-file call; the callee alone still honestly fails to compile. Also corrected a real, stale NORTHSTAR.md claim ("selfhost/lexer.prn still doesn't compile") that was actually just testing it without its own real stdlib/string.prn dependency -- confirmed it compiles with zero errors/warnings, and confirmed the full 5-domain selfhost pipeline + 3 stdlib deps already compiles and self-hosts examples/valid_only.prn end to end (an existing, working, previously-undocumented Makefile target, test-selfhost-main). Found and documented (not fixed) 3 real, separate selfhost/emit.prn gaps along the way: top-level defstruct isn't walked; a bare alloc/arithmetic/match/cond defn body falls through to a broken emit-tail-symbol fallback; plain-call let-values require every argument to be a bare symbol. Zero regressions across the full local suite (336 tests) + every selfhost domain's own test binary + bazel build //.... NORTHSTAR.md's own Self-hosting section updated with the full, accurate picture. (sess-20260825-1938-f6bd411e)

## 2026-08-27
- New redgarden mod: bacon_puck_intangible_speed_mod.prn — real PARENA decision logic (Arena API 'real logic' tier) computing the movement-speed bonus for Bacon+Puck's intangibility window. commit 96f1b83. (sess-20260825-1938-f6bd411e)
- Recovered two orphaned PARENA mods from an earlier isolated agent: stdlib/eventstore/seqlock.prn (cross-process file lock) and stdlib/redgarden/combat_log_mod.prn (combat-log v2 trigger layer). commit 4709fdd. (sess-20260825-1938-f6bd411e)
- Real fix for the large-file crash: windowed render-highlighted-text to the visible line range + added a separate per-frame arena that's reset every frame instead of sharing the persistent arena. Verified flat RSS over a 60s real repro (was unbounded growth before). commit e16076f. (sess-20260825-1938-f6bd411e)
- Terminal/editor toggle UI (fourth bottom-bar Toggle, shell/spawn + pty-poll-read per-frame drain, KeyDown/TextInput routing to pty-write): commit b841243. Windows pty stub fixing a real CI break: commit e1ee367. O_BINARY fix for a real CRLF-text-misdetected-as-binary bug: commit 33c6e4d. (sess-20260825-1938-f6bd411e)
- Linnen Settings panel v0: third bottom-bar Toggle (Settings) opens a Spotlight-style modal with a Zoom control (-/+ buttons sharing the existing zoom_percent adjust+clamp logic with Ctrl+scroll/Ctrl+±). commit 6522bf4. (sess-20260825-1938-f6bd411e)
- Mouse wheel now routes to whichever pane (file-tree sidebar vs code editor) the cursor is hovering over, instead of always scrolling the code buffer. Fixed Spotlight's own scroll-past-view bug: Down past row 11 used to move the real selection off-screen with nothing visible changing -- added spotlight_scroll_offset + spotlight_keep_selection_visible so the selection always stays in the rendered window. Full suite clean, CI green on all 4 jobs. PARENA commit c3afbea, Apple #16448. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- Fixed a real stack-overflow crash opening a large file: render-highlighted-text's own line walk was a plain self-recursive function call (one C stack frame per line, not a real loop/recur) -- a real 57,524-line file crashed within ~6 seconds. Rewritten as a real loop/recur, verified stable for 19+ seconds on the same file. Honest gap remains: full-file re-tokenization every frame is real, separate, unbounded work -- true windowed rendering not attempted. CI green on all 4 jobs. PARENA commit c946ae9, Apple #16444. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- Fixed a real O(n^2) crash/choke on large files: raw_read_all_impl (read-string's own host primitive) grew its buffer in fixed 4096-byte steps, copying the whole buffer on every grow and never freeing intermediate allocations. Fixed with fstat-sized single allocation + doubling-growth fallback. Verified live: a ~10MB file that previously couldn't finish reading in 30 seconds now reads in 0.045s; a real 133.5MB file reads in 0.526s. New wall-clock regression test. CI green on all 4 jobs. Honest scope: whole file still loads into memory at once -- true streaming/windowing for GB-scale files is separate, larger, unstarted work. PARENA commit 54652f2, Apple #16434. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- pty.prn: added pty-poll-read, a real non-blocking sibling to pty-read (poll(2) with a zero timeout before ever calling read(), never blocks) -- the real foundational fix needed before the editor's own terminal panel UI could be built at all (pty-read's own blocking contract would freeze the whole editor against an idle interactive shell). Also added shell/spawn (resolve + pty-open combined). 10 new assertions including a real wall-clock non-blocking check. CI green on all 4 jobs. PARENA commit 83bc177, Apple #16430. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- CI: 2 additional compressed Construct artifacts (PARENA_CONSTRUCT.tar.gz, PARENA_CONSTRUCT.zip) alongside the existing raw .txt, and the Construct file is now bundled into every real client release build (Linux/macOS tar.gz, Windows zip) so a downloaded build can be traced back to its exact source snapshot. Verified live against the real v0.97.0 release. PARENA commit c6411e2, Apple #16423. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- Spotlight: real Ctrl+T/Cmd+T command palette for the PARENA editor. New stdlib/editor/spotlight.prn: a real, plugin-extensible provider registry (run-providers) with file-search-provider and calculator-provider (reusing expr.prn, the founder's own explicit 'just a plugin' proof case) on the same real signature a third-party mod would use. Hot-swappable via the same drop-in-file+recompile PARENA-mod pattern already used elsewhere in this monorepo, not runtime dynamic loading. New sdl2/gui-held? for Cmd-key detection. Wired into examples/editor_main.c as a real modal overlay (Ctrl+T/Cmd+T, typed query, Up/Down selection, Enter to activate, Escape to close). 13 new assertions, full suite clean, CI green on all 4 jobs including real Windows/macOS builds. PARENA commit ea21508, Apple #16413. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- stdlib: consolidated 4 real, duplicated C FFI helpers found via an audit for 'one module rolling its own inline-c for something a shared module already provides' -- net/http.prn's i32-to-string (dup of string.prn's), json.prn/yaml.prn's parse-f64-raw (now one shared string/parse-f64-raw), awk.prn/grep.prn's unbox-bool (now shared in regex/pcre.prn), editor/textmate.prn's tm-match-start/tm-match-end (dup of pcre's own match-start-i32/match-end-i32, same underlying Match struct). Verified with real end-to-end targets (test-json/yaml/awk, turbogrep, test-editor-render's real Xvfb render, test-webdriver's real HTTP session), full suite + all 5 selfhost domains clean, zero regressions, CI green on all 4 jobs. PARENA commit d571624, Apple #16392. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- selfhost/emit.prn: emit-let-bindings now supports plain function-call let-bindings (not just alloc calls) -- the dominant real blocker found via a real self-compile attempt of selfhost/lexer.prn (35 of ~57 errors). New plain-call-shaped?/emit-plain-call/mangle-call-name (strips /-qualified names to their real final segment, matching how the full compiler never module-prefixes generated defn names); vec/-qualified calls stay a deliberate, permanent #error exclusion (same collision-risk reasoning src/emit.c's own mangle_call_name already applies). 13 new assertions (26 total for domain 4), CI green on all 4 jobs. Real, measured progress: lexer.prn's self-compile error count dropped 57 -> 46, still doesn't compile -- non-symbol call args, match/cond as body forms, and an undeclared-identifier class remain, documented in NORTHSTAR.md. PARENA commit b3d7071, Apple #16383. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- selfhost/emit.prn: emit-params now reads a param's own real declared C type instead of hard-coding Arena * for every param -- fixed a real, confirmed type-correctness bug (selfhost/lexer.prn's own new-lexer takes a String param, was being wrongly declared Arena *). New param-type-name/param-c-type helpers (String->char*, I32->int, else falls back to Arena*); only Arena-typed params still get added to the ArenaBinding scope. 7 new assertions (20 total for domain 4), CI green on all 4 jobs. Honest note: lexer.prn's own real self-compile error count is unchanged -- still dominated by unsupported non-alloc let-binding shapes, the next, larger step. PARENA commit 2e4bfc0, Apple #16379. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- selfhost/emit.prn: fixed a real, live segfault found via the first real self-compile attempt (build-file'ing selfhost/lexer.prn through the pipeline itself). emit-let-bindings called emit-alloc-call unconditionally on every let-binding, crashing on a plain function-call binding (a real, in-scope shape this narrow v0 emitter never supported). Fixed with a real alloc-call-shaped? guard: unsupported shapes now emit a clean C #error instead of crashing. 3 new regression assertions, CI green on all 4 jobs. Honest gap remains: lexer.prn still doesn't fully compile through the pipeline -- crash fixed, real language-coverage gap (match/loop/arithmetic/closures/generics) documented, not closed. PARENA commit 836e854, Apple #16374. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- selfhost/main.prn: real fifth self-hosting domain, a faithful port of src/main.c's cmd_build. build-file does a real disk-to-disk compile (read .prn file -> lexer -> parser -> region -> emit -> write .c file), completing the full pipeline as a real, callable, end-to-end build function. Found and fixed 2 real bugs: a region.prn/emit.prn private-helper-name collision (never exercised before both were compiled together), and a match-bound Ok defstruct payload needing deref when passed by value. 7 new assertions, CI green on all 4 jobs. PARENA commit 2249627, Apple #16364. session: sess-20260825-1938-f6bd411e (sess-20260825-1938-f6bd411e)
- Wired the self-hosting test suite (lexer/parser/region/emit) into real CI on all 3 platforms, closing a real, previously-flagged verification gap. Immediately found and fixed 2 real Windows-only bugs no local check could catch: SDL_main.h's own main() signature hijacking, then the resulting undefined WinMain link error once SDL2main wasn't actually linked. v0.89.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real fourth step -- selfhost/emit.prn completes a working PARENA-in-PARENA vertical slice (lexer -> parser -> region -> emit), scoped to VS0's own original narrow DoD bar. Verified by actually compiling and running the emitted C against the same driver used for the real C compiler -- real output, real assert, real pass. v0.88.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real third step toward a self-hosted PARENA compiler: selfhost/region.prn, a faithful PARENA-language port of src/region.c. Found and fixed 3 more real bugs including a genuine logic bug of its own (walk-let-bindings dropping outer scope bindings) caught by lifting the C reference's own real test cases verbatim. v0.87.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real second step toward a self-hosted PARENA compiler: selfhost/parser.prn, a faithful PARENA-language port of src/parser.c, 51 real assertions cross-checked against the C reference including exact DoD error wording. Found and fixed a real, deeper emitter gap: match-bound Err payloads never had their own type resolved (Ok/Some-only), causing a silent wrong-value bug caught by a real runtime check before shipping. v0.86.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real first step toward a self-hosted PARENA compiler: selfhost/lexer.prn, a faithful PARENA-language port of src/lexer.c, 60 real assertions cross-checked against the C reference. Found and fixed 2 real emitter truncation bugs (emit_if/emit_binop's fixed-size snprintf buffers silently corrupting deeply-nested expressions) along the way. v0.85.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real file-tree directory navigation: is-dir? (portable dirent.h host glue), sidebar entries color-coded dir/file, cd-into-directory on click + a synthetic '..' entry to go back up via new parent_dir_of. Caught and fixed a real absolute-path bug (last_slash<=0 conflating 'no slash' with 'slash at index 0') before shipping, via a new dedicated test suite. v0.84.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real NERDTree-style file-tree sidebar v0: new stdlib/io.prn list-dir (portable dirent.h host glue), examples/editor_main.c gets a fixed-width sidebar toggled by a second Toggle widget, click-to-open reuses the drag-and-drop spawn_new_instance infra, zoom-correct composition verified via 2 real screenshots. v0.83.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real UI widget system v0: stdlib/editor/widget.prn's Toggle type (defstruct + hit-test/flip/label/render), first real caller replaces the hand-rolled status-bar auto-indent toggle in examples/editor_main.c. v0.82.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real Ctrl+Zoom in the PARENA editor (matching PITVIPER's own convention) + real prnfmt-on-save (F2 runs the actual parena fmt formatter via a new runtime/prnfmt_bridge.c, solving a real Arena type collision and a real Arena symbol-name link collision); fixed a real screenshot-caught bug where the status bar rendered off-screen past ~130% zoom. v0.80.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Root-caused and fixed a real Windows DPI-awareness bug making mouse click/drag coordinates wrong in the PARENA editor (SDL_HINT_WINDOWS_DPI_AWARENESS=permonitorv2); added a hover-reveal status bar (auto-indent toggle) that stays hidden except near the bottom edge. v0.79.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real Ctrl+A select-all, mouse-wheel scroll (line-based), auto-indent-on-Enter (comment/string-aware paren-depth counter), and Tab-to-indent in the PARENA editor. v0.78.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real drag-and-drop file opening in the PARENA editor -- SDL_DROPFILE plumbed end-to-end, spawns a genuinely new editor process/window per dropped file (fork+execl/CreateProcessA); exe-relative font path lookup (executable_path/executable_dir) fixes the double-black-screen Windows launch bug the founder hit live. v0.77.0 (sess-20260825-1938-f6bd411e) (sess-20260825-1938-f6bd411e)
- Real macOS dylib bundling (otool/install_name_tool/codesign) for the PARENA editor release binary -- no longer needs 'brew install sdl2 sdl2_ttf'. v0.76.0 (sess-20260825-1938-f6bd411e)
- Real Markdown (.md) syntax highlighting in the PARENA editor -- headings/bold/italic/code/quotes/lists, auto-selected by file extension. v0.75.0 (sess-20260825-1938-f6bd411e)
- Real mouse-driven selection (click + click-drag) in the PARENA editor, plus a deep SDL2 event-queue timing finding from testing it. v0.74.0 (sess-20260825-1938-f6bd411e)
- Real Ctrl+Y redo in the PARENA editor, fixing a real spurious-duplicate-entry bug found while testing it. v0.73.0 (sess-20260825-1938-f6bd411e)
- Real Up/Down arrow-key cursor movement (fixing a real gap the founder hit live) + real Ctrl+Z undo in the PARENA editor. v0.72.0 (sess-20260825-1938-f6bd411e)

- Real Ctrl+C/X/V clipboard copy/cut/paste in the PARENA editor (SDL2's own real clipboard). v0.71.0 (sess-20260825-1938-f6bd411e)

## 2026-08-26
- Fixed real Windows editor-demo launch failure (hardcoded Linux-only font path caused instant crash) -- vendored JetBrainsMono-Regular.ttf, added RUN.bat + -mwindows to the Windows release. Shipped real text selection (Shift+Left/Right, Backspace/Delete/typed-replace act on selection, visual highlight). v0.70.0 (sess-20260825-1938-f6bd411e)
- Real per-platform (Windows/Linux/macOS) editor-demo binaries now attached to every GitHub Release; releases switched from --prerelease to regular. Fixed 3 real compiler/CI bugs found via live cross-platform CI (SDL2main Windows link, macOS SDL_ttf.h include, and a general emit.c uninitialized-match-result gap + unused-constructor warnings only Clang catches). v0.69.0 (sess-20260825-1938-f6bd411e)
- Made runtime.h + string.prn Windows-portable (ifndef _WIN32 guards on tcp/pty/process glue, portable contains-ci? impl), verified via real mingw cross-compile (sess-20260825-1938-f6bd411e)
- editor: fixed Home/End to be line-aware (was whole-buffer), now correct now that multi-line editing is real (sess-20260825-1938-f6bd411e)
- editor: real multi-line editing -- Return inserts a real newline, render-highlighted-text draws multi-line, real row/column cursor tracking, load switched to read-string (whole file). Verified with a real file round-trip (cat -A confirms a real newline byte on disk). (sess-20260825-1938-f6bd411e)
- editor: real file save/load (F2/F3), using the already-real stdlib/io.prn; found+fixed a second real instance of the bare-defenum-variant-collides-globally compiler bug (EventKind.Other -> UnhandledEvent) (sess-20260825-1938-f6bd411e)
- editor: real Home/End keys + forward-delete (move-cursor-home/move-cursor-end/delete-forward-at-cursor), wired into examples/editor_main.c (sess-20260825-1938-f6bd411e)
- editor/buffer.prn: real move-cursor-left/move-cursor-right, wired into examples/editor_main.c's event loop -- closes a gap left from earlier the same day (key-left/key-right constants existed but were never used) (sess-20260825-1938-f6bd411e)
- examples/editor_main.c: the real, standalone, runnable PARENA editor -- 'make editor-demo' builds a real SDL2 program with live keyboard-driven editing and TextMate-style syntax highlighting, the actual deliverable of this session's editor thread (sess-20260825-1938-f6bd411e)
- real syntax-highlighted PARENA source rendering: editor/theme.prn (scope->color) + editor/render.prn tie the tokenizer, real PARENA grammar, and SDL2 renderer together -- 4 real lines of actual PARENA source render as real colored text on screen (make test-editor-render). Also fixed a real VS0 emitter bug (bare 'unit' in a void-returning tail emitted 'return NULL;'). (sess-20260825-1938-f6bd411e)
- editor/buffer.prn: real keyboard-driven text buffer (Buffer{text,cursor}, insert-at-cursor/backspace-at-cursor), sdl2.prn's EventKind gained real KeyDown/TextInput payloads -- real edit sequence verified end-to-end through real SDL events (make test-editor) (sess-20260825-1938-f6bd411e)
- sdl2.prn: real text rendering (SDL2_ttf) -- ttf-init/open-font/render-text/measure-text-width/height, using PITVIPER's own real JetBrains Mono font, verified end-to-end under Xvfb (make test-sdl2) (sess-20260825-1938-f6bd411e)
- sdl2.prn: real host implementation + real renderer calls (create-renderer/set-draw-color/render-clear/render-fill-rect/render-present) -- first real slice of the PARENA-authored editor shell, verified with a real window+render test under Xvfb (make test-sdl2) (sess-20260825-1938-f6bd411e)

- pty.prn + shell.prn: real forkpty-based host implementation (spawn/read/write/resize/close, env lookup, PATH search, Git Bash detection) -- the concrete PARENA-eats-PITVIPER step, verified with a real end-to-end pty round trip (make test-shell) (sess-20260825-1938-f6bd411e)

## 2026-08-25
- turboawk dogfooding finished: expr.prn's parse-expr/EvalError/coerce-num/coerce-str + awk.prn's compiler gaps fixed, tests/test_awk.c 10/10 PASS against freshly-generated tests/test_awk_gen.c. Added 'make test-awk' (not a turboawk CLI -- expr.prn has no print/output primitive yet, honestly flagged, not built here). Full core suite (336 tests) + turbogrep/turbosed both still clean. Commit 8a5fcdd. (sess-20260825-1938-f6bd411e)
- tree_passive_mod.prn -- second real REDGARDEN PARENA mod (ARENA_HERO_TREE auto-attack passive) (sess-20260825-1938-f6bd411e)
- Real top-level (def) globals + current-arena + fn-lambda &mut/& reference params -- 3 compiler fixes closing ladybug's suite-tree gap. Full chain (webdriver.prn + ladybug + IDUNA spec) now compiles, links, and runs for real. (sess-20260825-0828-cc32a704)
- Real WebDriver-protocol client (stdlib/net/webdriver.prn) -- Selenium bindings, built on net/http.prn+json.prn. New stdlib/process.prn (real fork+exec FFI). 13/13 e2e checks against a real Go fixture server, spawned via PARENA FFI. (sess-20260825-0828-cc32a704)
- Real working HTTP client (net/http.prn) -- headers as parallel Vecs (no Map generics), url-free host/port/path API, live-verified against IDUNA :8080. (sess-20260825-0828-cc32a704)
- Real BSD socket host glue for net/tcp.prn (renamed API: tcp-connect/tcp-listen/tcp-accept/tcp-read/tcp-write/tcp-close) -- first working network I/O in PARENA, live-verified against IDUNA :8080. (sess-20260825-0828-cc32a704)
- Real json.prn and yaml.prn stdlib parsers, both live-verified against real inputs (json: 24 checks; yaml: 28 checks including a real bukkit.yml-shaped test); found and fixed 2 real compiler bugs (same-named #target self-recursion, and-argument loop misparse) along the way. (sess-20260825-0828-cc32a704)
- stdlib/redgarden/bloodflower_mod.prn: REDGARDEN's first real PARENA mod (on-moon-zenith), compiled via 'parena build' and linked directly into REDGARDEN's C binaries -- pure C-to-C, no cgo layer needed. Verified live: REDGARDEN's new tests/test_bloodflower.c exercises the real compiled mod end-to-end. (sess-20260825-0828-cc32a704)
- turbosed built: stdlib/regex/pcre.prn's `replace` (was a real stub, text unchanged) implemented for real via a #target escape-hatch pattern matching vec-i32-at/vec-string-at; stdlib/sed.prn's `substitute` fixed (stale read-line arity, nested-match compiler-gap workaround matching grep.prn's own). Two real bugs found+fixed via full-corpus diff against real GNU sed (not assumed correct): find-all's substring-relative-position bug (hung/OOM'd on multi-match input) and a resulting off-by-one that silently skipped every second adjacent match (s/e/E/g failed 43/68 real .prn files before the fix, 0/68 after). Added explicit global? param (real sed's s/// vs s///g distinction) rather than defaulting to always-global. tools/turbosed_host.c + tools/turbosed-router.sh (same layered-safety shape as turbogrep's own) + `make turbosed`; deliberately NOT symlinked into ~/.local/bin/sed (sed mutates real pipelines, higher bar than grep) -- see docs/TURBOSED_VERIFICATION_REPORT.md. turboawk not attempted: checked this repo's own real awk usage first (one pattern, `awk '{print $1}'`), scoping note left for whenever it's picked up. (sess-20260825-0828-cc32a704)
- README: added a real section documenting parena ci-status and the two-stage bootstrap build, verified fact-by-fact against stdlib/ci/status.prn and the Makefile. Commit e940d58. (sess-20260824-2252-ce890e4f)
- Forward-declaration typing gap fixed: src/emit.c's defn pre-pass now emits fully-typed prototypes (resolve_param_prototype_type/build_defn_prototype) instead of empty-parens/K&R-style, so gcc catches by-value/pointer call-site mismatches at compile time (the exact bug class behind the Star segfault, fixed separately in 9a622f5). Commit 99ef91a, Apple #15728. (sess-20260824-2252-ce890e4f)
- Plus/Optional regex quantifiers implemented (match-plus/match-optional in stdlib/regex/pcre.prn); found+fixed a critical live segfault in Star dispatch (by-value where a pointer was expected, undetected by gcc due to empty-parens forward declarations). Commit 9a622f5, Apple #15726. (sess-20260824-2252-ce890e4f)
- bottleneck audit doc updated: C1 (CharClass) and C2 (Anchor) both marked fixed (sess-20260824-2252-ce890e4f)
- Anchor (^/$) implemented in regex/pcre.prn -- verified against real grep, router now routes anchored patterns to turbogrep instead of falling back (sess-20260824-2252-ce890e4f)
- grep router rewritten flag-safe (delegates any flag/stdin usage straight to real grep) + installed at ~/.local/bin/grep + fixed a real symlink-resolution bug (dirname doesn't follow symlinks) (sess-20260824-2252-ce890e4f)
- turbogrep router (tools/turbogrep-router.sh): falls back to real grep on unsupported patterns via a real AST-based capability check (exit code 3), logs every invocation to NDJSON for future usage-driven prioritization (sess-20260824-2252-ce890e4f)
- CharClass ([abc]/[a-z]/[^abc]) implemented in regex/pcre.prn -- closes the most dangerous bottleneck-audit finding (was silently matching zero lines) (sess-20260824-2252-ce890e4f)
- full bottleneck audit before further point fixes: 7 perf items + 13 correctness gaps + 3 compiler hazards, each verified or explicitly flagged unverified (sess-20260824-2252-ce890e4f)
- literal fast path in is-match: turbogrep another 4x faster (0.69s -> 0.168s), ~75x faster than original baseline, ~8x slower than system grep (was ~430-660x) (sess-20260824-2252-ce890e4f)
- buffered read-line: 3,786x fewer read() syscalls (root-caused via strace), turbogrep 18x faster (12.6s -> 0.69s, ~23x slower than grep instead of ~430-660x) (sess-20260824-2252-ce890e4f)
- turbogrep verification report: 7/7 patterns byte-identical vs real GNU grep on a 949-file/213K-line monorepo corpus; tools/turbogrep_host.c + 'make turbogrep' added (sess-20260824-2252-ce890e4f)

- io.prn real host implementation (POSIX raw-fd glue) + 2 real compiler bugs fixed (bang-insensitive scope_lookup, literal true/false-as-bind-name guard) -- grep.prn verified end-to-end, matches correctly against real files (sess-20260824-2252-ce890e4f)

## 2026-08-24
- parena fmt [-w] -- gofmt-style auto-formatter, comment/string-preserving text-level re-indenter (not AST round-trip, ast.h has no comment nodes) (sess-20260824-2252-ce890e4f)

- regex/pcre.prn rewritten closures-free (VS0's fn literals can't capture) -- compile/is-match/matcher internals gcc-clean; grep.prn updated to match; 2 real emit.c bugs fixed (set! ref-var deref-assign, struct-literal generic-pointer cast); io.prn's own missing host glue is the remaining end-to-end grep blocker (sess-20260824-2252-ce890e4f)

## 2026-08-23
- fix(emit): let-binding value now recognizes match/loop as special forms -- real root-cause fix (not a workaround) for one of the 5 nesting gaps found building regex/nfa.prn. make test 336/336 + test-multifile + test-domain4 all clean, zero regressions. (sess-20260823-1554-932e157f)
- feat(regex): real, working Pike's VM runner (is-match) for regex/nfa.prn -- unanchored breadth-first thread-stepping search, no backtracking anywhere. 35/35 runtime test cases pass, zero ASan findings. Found/worked around 5 more real compiler nesting gaps (match not recognized as a special form in when-body/let-value/cond-test/non-tail-do-statement positions, plus a deep-nesting result-var corruption). (sess-20260823-1554-932e157f)
- feat(regex): real, working Thompson-construction NFA compiler (compile: AST -> bytecode) for regex/nfa.prn -- literals, concat, alternation, quantifiers, groups, char classes, anchors. 31/31 runtime test cases pass, zero ASan findings. is-match/find/find-all (the Pike's VM runner) still real, deferred follow-up. (sess-20260823-1554-932e157f)
- feat(regex): real, working recursive-descent parser for regex/syntax.prn -- literals/alternation/quantifiers/capture groups/char classes/anchors/escapes, gcc-clean, ASan-clean, 13/13 well-formed + 4/4 malformed test cases correct. Real remaining gap for grep/sed/awk: regex/nfa.prn's matcher (Thompson NFA + Pike's VM) is still unbuilt. (sess-20260823-1554-932e157f)

- feat(tools): vim syntax highlighting for .prn source (tools/vim/); feat(emit): self-referencing defenum/defstruct type support + box-helper decl spacing fix, scarab.prn registration redesign to explicit builder -- make test 336/336 passing (sess-20260823-1554-932e157f)

## 2026-08-21
- dataframe.prn 全檔完工:select 修好後也真的能跑了。修好兩個新缺口:resolve_declared_type 對 (&Type) 的括號解析、match void-typed clause 的宣告+assign 問題。新增 331→336 個迴歸測試。Apple #15271,commit 4dbd60f。 (sess-20260820-0649-a3f19d93)
- compress/lz4.prn 改成真正、純 PARENA 的 LZ4-style 壓縮器(不再是 FFI stub);round-trip 驗證正確含 overlap-copy 情境。補齊 &mut (ComplexType) 參數 + loop 空結果 return 兩個編譯器缺口。新增 325→331 個迴歸測試。Apple #15268,commit 923a24c。 (sess-20260820-0649-a3f19d93)
- 修好兩個真實、通用的編譯器 bug:單一 token &name 的 hint 查找、vec_get 對指標型別元素的雙重間接參照。column 重新驗證正確;vec_test.prn/world_test.prn 修好並跑過 firefly/run-tests。select 卡在新的第三個缺口,誠實記錄。新增 319→325 個迴歸測試。Apple #15263,commit 741ce36。 (sess-20260820-0649-a3f19d93)
- 新增 (return expr) 非局部控制流(真正的 C return,無需傳遞邏輯);column.prn 補齊 dest 參數 + ColumnNotFoundError,gcc 乾淨編譯;select 卡在 String 元素 vec_get hint 的新缺口,誠實記錄。新增 317→319 個迴歸測試。Apple #15249,commit 4df1176。 (sess-20260820-0649-a3f19d93)
- nn.prn 全檔編譯過關並用真實數值驗證正確(layernorm/gelu/softmax/relu/leaky-relu/sigmoid/tanh-activation)。新增 exp-of/tanh-of(#target FFI 包 libm)。新增 311→316 個迴歸測試。Apple #15247,commit 7f4e9a5。 (sess-20260820-0649-a3f19d93)
- firefly/ladybug 真的能跑了(F64 特化,matcher 改為 tagged union);新增真實 BDD 範例測試(4/4 通過);補齊 4 個通用編譯器缺口(多欄位 match 解構、單欄位型別追蹤、裸 defn-name 當值、struct 值的 vec/push! boxing)。新增 301→311 個迴歸測試。Apple #15245,commit f1e84eb。 (sess-20260820-0649-a3f19d93)
- 新增 Vec 字面值 + unwrap;用執行期 harness 抓到 loop 變數 int/double 混淆的真實缺口(誠實記錄未修)。新增 296→301 個迴歸測試。Apple #15240,commit 3d49190。 (sess-20260820-0649-a3f19d93)
- 補齊 glob.prn 缺少的 char-eq?/char-at-eq?/match-bracket-class,用 17 組真實 case 驗證正確。Apple #(見上),commit be56d5b。 (sess-20260820-0649-a3f19d93)
- array.prn 全檔編譯過關並驗證數值正確:新增 unary not、vec-eq?、間接呼叫回傳值 boxing;修好 strides-for 真實 bug(stride 推入順序反了)。新增 272→290 個迴歸測試。Apple #15234,commit 55d13d6。 (sess-20260820-0649-a3f19d93)
- 新增 fn lambda 字面值支援(非捕獲、需明確型別標注),修好 array.prn 的 add/mul-elementwise 缺口;新增 278 個迴歸測試。Apple #15231,commit 5312f6e。 (sess-20260820-0649-a3f19d93)
- 補齊 STDLIB.md 記錄的 parse-i32 + array/get/set! 缺少 dest Arena 參數缺口(比照 reshape/serve 作法);新增 272 個迴歸測試涵蓋此形狀。Apple #15228,commit 33da3e1。 (sess-20260820-0649-a3f19d93)
- ci-status 接入 parena CLI 本體(兩階段 bootstrap:.parena-bootstrap 產生模組 C,PARENA_HAS_CI_STATUS 重編 main.c 連結進最終 binary);移除已取代的 tools/ci_status_main.c 獨立 binary。Apple #15225,commit ff93388。 (sess-20260820-0649-a3f19d93)
- 新增 stdlib/ci/status.prn(native PARENA CI 查詢模組)+ tools/ci-status binary,真正 dogfood PARENA 取代 python3 one-liner;過程中自己抓到並修好兩個真實 JSON 格式假設 bug;已用它自己輪詢真實 CI 到通過;下一步 wire 進 parena CLI 本身;Apple #15221,commit 269e090 (sess-20260820-0649-a3f19d93)
- 新增 CI 自動 minor version bump + GitHub Release 機制(--prerelease,VS0 還在 pre-1.0);已驗證真的建出 v0.1.0;Apple #15216,commit be7a964/ff4f251 (sess-20260820-0649-a3f19d93)
- 補齊 pty.prn 的 Pty/PtyError 型別定義;修好 shell.prn 自己原始碼裡的 getenv/libc 撞名 bug(改名 env-lookup);pty.prn+shell.prn 組合編譯剩下的都是已知 FFI 缺口;Apple #15211,commit 96aabe6 (sess-20260820-0649-a3f19d93)
- 修好 current-arena(reshape/serve 改用明確 dest 參數,沿用 skip 已有做法);emit_loop_tail 新增 match 分支(遞迴 emit_match_core,貫穿 loop_locals);emit_match_clause_body 新增 recur 分支;自己抓到並修好純值 clause 巢狀在 loop 尾端缺少 break 的 bug;net/http.prn serve 現在只剩已知 FFI 缺口;Apple #15209,commit 9fd6510 (sess-20260820-0649-a3f19d93)
- loop 支援直接當 match clause body(emit_loop_core 從 emit_loop 拆出來,共用 result_var);修好丟棄陳述式的通用 -Wunused-value 缺口(所有丟棄的陳述式包上 void(...));net/http.prn 的 serve 只剩 current-arena 一個獨立缺口;Apple #15206,commit 2932a95 (sess-20260820-0649-a3f19d93)
- 補齊 net/tcp.prn(TcpListener/TcpStream/NetError)+ net/udp.prn(UdpSocket/SocketAddr/NetError)缺少的型別定義;net/http.prn get/post 推進到第三道缺口(未設計的 HTTP helper 函式);serve 另有 current-arena + loop-as-match-clause-body 兩個獨立缺口;Apple #15204,commit b194864 (sess-20260820-0649-a3f19d93)
- (Map K V) 型別擦除(erased to void*,無真實 runtime 實作)+ Fn 型別參數列表支援裸 Arena(解析成 Arena *);net/http.prn 錯誤前緣推進到 net/tcp.prn 的多檔案依賴牆;Apple #15201,commit 8325a08 (sess-20260820-0649-a3f19d93)
- match 支援巢狀(拆成公開入口 + 可遞迴的 core,共用 result_var)+ emit_loop_tail when 非尾端陳述式委派給 emit_body;同一類「raw emit_expr 用在需要陳述式分派的地方」缺口的第三個出現位置;match result_var 補 unused attribute;Apple #15199,commit 49c2f59 (sess-20260820-0649-a3f19d93)
- 修好 vec/get 被誤解析成使用者自己同名 get 函式的真實 regression(自己抓到,vec 前綴明確排除在裸名字解析之外);補上 unit 字面值(Unit 型別單例值,輸出 NULL);Apple #15196,commit bea812f (sess-20260820-0649-a3f19d93)
- string.prn 補齊 char-at/str-eq?/is-digit?/substring/ParseError(原始設計,STDLIB.md 沒涵蓋)+ 修好三個原始碼 bug(substring 誤用 dest 參數、char-at 無效三參數形式)+ #include <stdlib.h>;10/11 函式真正 gcc-clean,parse-i32 卡在已知的 stdlib 設計問題;Apple #15193,commit e36ed21 (sess-20260820-0649-a3f19d93)
- 新增 mangle_call_name() 修正跨模組 module/function 呼叫解析——之前一律誤 mangle 成從未存在的名字,現在靠 g_defn_return_types 判斷裸名字是否為真實 defn,保守 fallback 保護 vec/push!、string/concat 等既有正確案例;影響遍及 map/array/io/stats/sdl2/pty 等整個 stdlib;Apple #15188,commit b139bd9 (sess-20260820-0649-a3f19d93)
- 新增 defn 前向宣告 pre-pass,修好函式間跨行前向參照(string.prn/regex/glob.prn 之前誠實記錄的缺口);舊式空括號 C 宣告,不複製完整參數型別解析邏輯;順手提早註冊 g_defn_return_types;Apple #15169,commit 7036ce1 (sess-20260820-0649-a3f19d93)
- cond 特殊形式(純值三元折疊 + loop-tail 陳述式版,含自己抓到的結果型別 bug 修正)+ if-in-tail 陳述式化 + alloc 尺寸運算式 + mid-body #target + #include <stdint.h>;string.prn/regex/glob.prn 卡在下一層獨立缺口(函式前向參照排序、未定義型別/函式),誠實記錄未解;Apple #15166,commit 03a6c56 (sess-20260820-0649-a3f19d93)
- Ok/Err/Some 非指標 payload 裝箱(生成 per-type box 輔助函式)+ when-in-loop-tail 處理 + 純量裝箱判斷改看值型別而非 hint;補齊 array.prn 的 ShapeError/IndexError 型別定義;product/strides-for/zeros/flat-index/from-vec 五個函式真正 gcc-clean;get/set! 卡在新的、更深的 stdlib 設計缺口(誠實記錄,未解);Apple #15163,commit 3735d28 (sess-20260820-0649-a3f19d93)
- 修正非引用型 (Vec T) @ Region 參數形狀(array.prn 的 zeros/from-vec/reshape);product/strides-for/zeros 完全編譯過關;發現並記錄 Ok/Err/Some 指標型別 payload 限制這個獨立、更深的缺口(需要 arena 裝箱機制,STDLIB.md 已誠實記錄);Apple #15160,commit 899bef0 (sess-20260820-0649-a3f19d93)
- 里程碑:world.prn 真正通過真正 gcc 編譯。這一輪抓到四個獨立、真實的缺口:N-ary and/or(左結合折疊,只針對邏輯運算子)、when 特殊形式(真正的陳述式層級 if、不是三元運算子)、mangle() 補上問號結尾的 predicate 命名慣例、Vec 純量裝箱(讓 F64/I32 元素真正配置成真的可解參考的 cell,不是 bit-boxing 技巧,跟既有 deref 慣例完全一致)。順手補上 vec-set-at! 這個 STDLIB.md 自己設計文件裡本來就漏掉的操作 (sess-20260820-0649-a3f19d93)
- io.prn 補齊缺失的 FileHandle/IoError 型別定義，跟 pcap.prn 同一類缺口——型別現在能正確解析，#target host glue 本身還是誠實地留著沒實作（跟這個 stdlib 裡其他 FFI 綁定檔案一樣） (sess-20260820-0649-a3f19d93)
- 修好 mangle() 的開頭驚嘆號處理——之前錯誤地轉成底線（_t/_m/_cap），但多個真實 stdlib 檔案自己手寫的 #target inline-C body（thread.prn 的 lock、pcap.prn 的 read-packet/filter）都是用去掉驚嘆號後的原始名稱（m、cap）直接引用參數，改成整個去掉開頭驚嘆號才對；中間/結尾的驚嘆號（vec/push!、set!）維持轉底線不變。順手把 pcap.prn 自己真正缺的 Capture/Packet/PcapError 型別定義補上——pentest/* 六個檔案現在全部真的能編譯 (sess-20260820-0649-a3f19d93)

- 真實修好 defenum/defstruct 前處理順序 bug——用新的多檔案編譯測真正的 ladybug/scarab 組合時發現：原本把整個程式所有 defenum 一次處理完才輪到所有 defstruct，不管真實檔案順序，導致 scarab.prn 的 SuiteNode（Spec 變體用到 firefly.prn 定義的 T）明明 T 排在前面卻找不到。改成合併成一個 pass、照真實合併後的檔案順序處理 (sess-20260820-0649-a3f19d93)

## 2026-08-20
- 重大里程碑：parena build 支援多檔案編譯（parena build a.prn b.prn -o out.c），真正把多個 .prn 檔案的頂層形式合併成同一個編譯單元——這正是這整個 session 一直撞到的 ladybug/scarab 模組連結缺口的真正、架構層級解法。真實驗證：多檔一起編譯出真正 gcc 乾淨的 C，同一個檔案單獨編譯依然誠實失敗（證明不是巧合）。新增 run_multifile_check.sh 整合測試，接進 Makefile 跟 CI。順手發現的新缺口：firefly.prn+ladybug.prn 一起編譯後推進到匿名函式/closure（(fn [args] body)）這個全新、獨立、還沒開始的缺口 (sess-20260820-0649-a3f19d93)
- 多欄位 defenum variant payload——scarab.prn 的 SuiteNode 真的能過這關了，往前推進到跟另外兩個 ladybug 檔案一樣的同一個既有模組連結缺口 (sess-20260820-0649-a3f19d93)
- 里程碑:firefly.prn(ladybug BDD 框架的地基)真的通過真正 gcc -Wall -Wextra -pedantic -Werror 編譯——這一輪額外抓到並修好五個真實 bug(呼叫一等函式值、defstruct 的 Fn 欄位語法接錯位置、emit_call 對已知函式回傳型別的猜測蓋掉了正確答案、deref 少轉型導致對 void* 直接解參考、sb_appendf 固定 1024 bytes 緩衝區默默截斷過長的函式本體),外加補上 g_vec_elem_hints(Vec 元素型別追蹤)跟真正的 string_concat runtime 實作 (sess-20260820-0649-a3f19d93)
- map-literal struct construction(STDLIB.md 缺口 #2)+ let/do 在 loop tail position 的真實結構性 bug 修復,firefly.prn 的 run-tests 又推進一步,現在卡在更深的 Vec 元素型別遺失問題(deref (vec/get ...) 拿不到真正的 TestCase 型別,只剩 void) (sess-20260820-0649-a3f19d93)
- STDLIB.md 新增 fp 套件設計(Ramda 對等的 FP toolkit:map/filter/reduce/compose/pipe/prop/pluck/predicate combinators),搭在這次新落地的 (Vec T) 之上;auto-currying 誠實標記為尚未開始(需要真正的 closure 表示法) (sess-20260820-0649-a3f19d93)
- emit.c 新增 &Type/&mut T 參考型別、(Vec T)、do/set!/deref 特殊形式、get-field 自動穿透參考;修掉 has_region_marker() 的真實既有 bug(任何 Type @ Region 都被誤判成 Arena *,不只 Arena 本身)+ ISO C99 void 函式 return 表達式的真實 gcc -pedantic bug;test_emit.c 95→121;firefly.prn 真的通過 Vec 欄位那關,推進到下一個已知、獨立的 gap (sess-20260820-0649-a3f19d93)
- STDLIB.md 新增 container/docker 設計(Docker Engine API,搭在 net/http 之上),明確連結到 Emily OS backlog(S189-56d)當作實際部署 workload 的容器後端 (sess-20260820-0649-a3f19d93)
- STDLIB.md 新增 container/lxc + container/cgroup 設計(LXC 生命週期 + cgroups v2 真實檔案系統介面),回應 Moltbook/OpenClaw hardening 討論;順手修正 firefly/gomega 改名後遺留的過期命名 (sess-20260820-0649-a3f19d93)
- 改名 firefly/gomega → firefly/ladybug(甲蟲命名慣例,呼應 firefly/scarab;gomega.prn 保留為 back-compat alias,exported API 不變,scarab.prn 的 import 已跟進改用新名);查證 firefly/ladybug/scarab 三檔各自仍卡在既有已知 gap(&Any 參考型別、回傳型別、multi-field defenum payload),額外發現 VS0 emitter 目前尚無 module/import/export 真實連結機制 (sess-20260820-0649-a3f19d93)
- LONGMA(streaming CSV chunker,github.com/emilyspringerton/longma)功能真正移植進 PARENA:新增 stdlib/csv.prn,並在 longma fork 本身建立完整 Bazel+PARENA 交叉 repo 建置(MODULE.bazel git_override 釘死 commit、BUILD.bazel genrule+cc_library、CI workflow),本地驗證 bazel clean 後重新建置零警告全過;遠端 CI 因 GitHub API rate limit 待後續確認,誠實記錄 (sess-20260820-0649-a3f19d93)
- resolve_declared_type 支援 Bool/F64,修好 parena_runtime.h 缺少的 OK/ERR 巨集(6 個 stdlib 檔案、18 個真實呼叫點依賴這個),gfd.prn(METALVERSE)整個檔案現在完整編譯成功並用真正 gcc 驗證零警告;CI 綠燈 (commit c9fc3b5) (sess-20260820-0649-a3f19d93)
- VS0 domain 5(CLI Runner)完成 + 自動化驗證,全部 5 個 DoD domain 收尾;新增 tests/integration/run_domain5_check.sh 接進 CI;修好一個過程中自己引入的 CI YAML 冒號解析 bug;CI 綠燈 (commit 54d1ae7,修復 c308f20) (sess-20260820-0649-a3f19d93)
- 新增 STDLIB.md 缺口盤點文件:mapbuilder/tools.prn + world.prn 真實測試後找出的 8 個 VS0 編譯器缺口(多欄位 defenum payload、map 字面量 struct 建構、命名空間化 variant 建構、Vec 操作、參考型別、tuple 型別、set!、F64),規劃性質,尚未實作 (sess-20260820-0649-a3f19d93)
- 泛用 Arena @ Region 支援(參數+回傳型別)+ 新增 pentest stdlib(nmap/Wireshark/sqlmap+Nikto/Aircrack-ng/John the Ripper+Hashcat/Metasploit FFI 綁定,5/6 檔案完整編譯成功);CI 綠燈 (commit d5456d1) (sess-20260820-0649-a3f19d93)
- 新增 defenum:真正的使用者自訂 tagged union(重用 Result/Option 的 {tag; void *value;} 形狀),讓 stdlib/editor/events.prn 完整編譯成功;CI 綠燈 (commit 4830d02) (sess-20260820-0649-a3f19d93)
- editor_plugin.prn 進 examples/,新增真正的 Bazel target(genrule 產生 C + cc_library 真的編譯驗證,DoD 同一套旗標);CI 綠燈 (commit 90a8cfa) (sess-20260820-0649-a3f19d93)
- (Fn [] ..) callback 泛化支援任意回傳型別 + 新增 cache stdlib(Russian doll nested caching,Rails 真實先例);CI 綠燈 (commit 24f82b3) (sess-20260820-0649-a3f19d93)
- emit_defn 支援不帶 region 標註的純 I32/String 參數,對應 stdlib/editor/ui.prn 的行號/像素座標這類參數形狀;CI 綠燈 (commit cf14d96) (sess-20260820-0649-a3f19d93)
- emit_defn 新增 #target/inline-c FFI 逃逸機制 + 明確回傳型別標註(Unit/I32/String/(Result ..)/(Option ..)),讓 stdlib/editor 的 mod-surface plugin 介面檔案能真正被辨識;CI 綠燈 (commit aa25d6b) (sess-20260820-0649-a3f19d93)
- emit_match：新增 Result/Option 上的模式匹配支援（Ok/Err/Some/None），含新的執行期 Result/Option 型別與建構輔助函式；CI 綠燈 (commit 64f5527) (sess-20260820-0649-a3f19d93)
- C emitter 加入 loop/recur(真正的迴圈,含真實同時賦值語意),朝自我託管邁進一步。本地 Makefile+Bazel+ASan/UBSan+domain4 全數驗證通過;CI 確認因 GitHub API rate limit 暫時無法查詢,誠實標註待確認 (sess-20260820-0649-a3f19d93)
- C emitter 加入算術/比較/if/一般函式呼叫,朝自我託管邁進一步。CI 已確認綠燈(run 32384381363) (sess-20260820-0649-a3f19d93)
- pitviper/tiling(真實 i3wm 互動模型)stdlib 設計 (sess-20260820-0649-a3f19d93)
- pentest/* 六個套件(Kali 對應工具集)、idvault + pitviper/expand(刻意淺層設計)、依賴關係圖完整重新整理(57 個套件) (sess-20260820-0649-a3f19d93)
- 六個新 stdlib 套件設計:media/tts(F5-TTS sidecar)、pitviper/quicklook(macOS Quick Look 互動模型)、net/vpn+packetradio+mesh(WireGuard/AX.25+APRS/Meshtastic)、cli+config(Cobra/Viper 對應,自我託管的真實第一目標) (sess-20260820-0649-a3f19d93)
- VS0 domain 4(記憶體驗證)完成 + CI 修復(ASan/Valgrind 物件檔混用 bug);新增 pitviper/protocol + compress/lz4 + profile + staticanalysis + git 五個新 stdlib 套件設計 (sess-20260820-0649-a3f19d93)
- 新增 firefly(基礎測試函式庫,Go testing.T 形狀)+ firefly/gomega(matcher chain)+ scarab(BDD + test runner,Ginkgo 形狀),甲蟲命名;開始為 vec/map/world 寫真實測試 (sess-20260820-0649-a3f19d93)
- VS0 domain 2(region analyzer)真正實作完成:單一遍歷 scope 追蹤器,通過 NORTHSTAR DoD 自己的正例/反例驗收(逐字相同的錯誤訊息、正確行號),8 個測試涵蓋真實邊界案例,ASan/UBSan 乾淨,CI 綠燈已用 API 直接確認 (sess-20260820-0649-a3f19d93)
- 新增 mapbuilder(tools/layout/template)+world 資料模型、pty/shell/ssh/crypto 標準函式庫(具體 dogfood 進 PITVIPER 路徑)、gfd/browser(FFI 綁定真實瀏覽器引擎)、NORTHSTAR 記錄 V8 JIT 長期構想 (sess-20260820-0649-a3f19d93)
- 新增 ringo(matplotlib 對應套件,依 array + sdl2),真實 .prn 原始碼已通過 parena parse 驗證 (sess-20260820-0649-a3f19d93)
- 標準函式庫大量擴充:regex 全引擎家族、grep/sed/awk、vec/map、net tcp/udp/http、內建 sdl2、editor/*、otp (Erlang 風格 + scheduler)、media (audio/codec/stream)、sql 建構區塊、gpt2 移植數值套件全數轉為真實 .prn 原始碼(parena parse 驗證通過);NORTHSTAR 加入創辦人使命宣言;PITVIPER 修復 WSL 誤判 bug (sess-20260820-0649-a3f19d93)
- STDLIB.md: multi-engine regex family (regex/syntax, regex/nfa linear-guarantee, regex/pcre full backtracking, regex/posix, regex/glob) plus grep/sed/awk built on top; new expr + io/read-line dependencies; explicit batteries-included scope note (sess-20260820-0649-a3f19d93)
- Migrated to Bazel as primary build system (bazelisk installed, MODULE.bazel/BUILD.bazel/.bazelrc with a real --config=asan, CI updated, verified green). Extended stdlib design: dataframe (pandas-equivalent), nn/tokenizer/sort + io/read-floats (grounded in porting the real gpt2-alpine-c source). (sess-20260820-0649-a3f19d93)

- VS0 lexer + parser + CLI implemented, satisfying NORTHSTAR.md's Definition of Done domain 1 (32 unit tests, balanced + imbalanced S-expressions). CI green (build, test, ASan/UBSan, real test.prn smoke test). (sess-20260820-0649-a3f19d93)

